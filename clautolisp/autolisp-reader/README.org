#+TITLE: autolisp-reader
#+AUTHOR: Codex
#+OPTIONS: toc:2 num:t

* Purpose
~autolisp-reader~ is the reader subsystem for the ~clautolisp~ implementation.

Its responsibility is to accept AutoLISP source text, decode it at the input boundary, tokenize and parse it according to an explicit dialect configuration, and return source-aware reader results without leaking Common Lisp reader semantics into the language.

* Main Files
- ~PLAN.md~
- ~documentation/autolisp-reader-specifications.org~
- ~documentation/autolisp-reader-user-manual.org~
- ~tools/read-autolisp/README.org~

* Status
This module is in specification-first mode.

The first implementation work should follow the syntax and data-model decisions recorded in ~documentation/autolisp-reader-specifications.org~.

The current user-facing API is documented in ~documentation/autolisp-reader-user-manual.org~.

The batch validation tool is documented in ~tools/read-autolisp/README.org~.
