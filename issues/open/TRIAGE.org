# -*- mode:org; coding:utf-8 -*-
#+TITLE: clautolisp — issue triage & processing index
#+TODO: TODO(t) NEXT(n) BLOCKED(b) DEFERRED(d) | DONE(D) WONTFIX(w)
#+STARTUP: overview

Authoritative triage of =issues/open/=. Replaces the old =index.txt=
(its content is preserved in git history; historical processed batches
live there and under =issues/closed/=). Rebuilt 2026-08-02 against the
real open set (65 issues at =fe30b9e=); re-synced 2026-08-09 at
=528578c= (72 open issues) — the four alpm-campaign / packaging issues
filed on 2026-08-08..09 are now indexed, and a full diff of
=issues/open/*.issue= against this file showed no other drift (the
seven =opendcl-*= are indexed in prose under P3, and the four rows
whose files are gone — cad-path-dotdot-resolution,
deferred-bricscad-setq-nil-returns-2, pi-constant-unbound,
ssget-implied-selection-not-graphical — are DONE and already moved to
=issues/closed/=, kept here as record). Re-synced again 2026-08-15 at
=cc3e227= (69 open issues, after MR !80/!81/!82): a full slug-by-slug
diff of =issues/open/*.issue= against this file found exactly two
unindexed issues, =navi-double-quit= and =navi-sedit-eval-command=
(both pjb DRAFTs, now indexed under Interactor / UX), and no orphan
rows — all 18 rows whose file is gone are accounted for in
=issues/closed/=. Priority order set by pjb:
*P1 CAD backend / dialect divergences → P2 language / builtins
completeness → P3 everything else.*

* Processing rules

- If an issue is completely done, close it and move it to =issues/closed/=.
- Process issues in the priority order below (P1 → P2 → P3); within a
  tier, =NEXT= first, then =TODO=; skip =BLOCKED= / =DEFERRED=.
- Strive to implement an issue completely. Only inconsistency or a
  dependency (missing spec, or another issue's implementation) justifies
  putting it on hold and moving on.
- Before starting, check the tree: progress may have made parts (or all)
  of an issue irrelevant — then close it with an explanation.
- Non-blocking bugs found mid-issue: enqueue as a new issue (standard
  format below) and add it here. Blocking bugs: fix within the current
  issue (may be a separate commit).
- Branch naming: bug → =fix-<slug>=, feature → =feature-<slug>=; an epic
  runs in one long-lived branch for its sub-issues. Fetch + rebase before
  merging back to =master=.

* Standard issue format

Every issue file is =<slug>.issue=, org-mode, UTF-8, beginning with the
keyword header below; new issues MUST use it, existing ones are
retrofitted as they are touched.

#+begin_src org
  # -*- mode:org; coding:utf-8 -*-
  #+TITLE: <slug> — <one-line summary>
  #+STATUS: NEXT|TODO|BLOCKED|DEFERRED|DONE|WONTFIX
  #+PRIORITY: P1|P2|P3
  #+TYPE: bug|divergence|cad-runtime|feature|epic|spec|infra|doc
  #+DEPENDS: <issue slug(s) or external gate, or ->
  #+BRANCH: fix-<slug> | feature-<slug> | <epic-branch>
  * Context
  * Problem
  * Proposal
  * Acceptance criteria
#+end_src

=BLOCKED= records *why* in =#+DEPENDS:= (e.g. =bricscad-access=,
=windows-access=, =pjb-decision=, =libredwg-upstream=, or an issue slug).

* P1 — CAD backend / dialect divergences
  :PROPERTIES:
  :Priority: P1
  :END:

| Issue                                         | Type            | Status   | Note |
|-----------------------------------------------+-----------------+----------+------|
| vendor-probe-autocad-bricscad-divergences     | divergence-cat  | TODO     | canonical AutoCAD⇄BricsCAD divergence register; anchor for the rest. All three divergences RESOLVED in clautolisp: D1 DONE (1.6.16), D2 closed (contamination), D3 DONE (1.6.17, entmod-on-XRECORD per-dialect, verified green 2026-08-06). Remaining is NOT clautolisp code: the D1/D3 cadaptor forks (external repo) + selection re-verify blocked on regenerating a clean empty.dwg on the Windows runner |
| bricscad-dialect-sysvar-parity                | dialect-bug     | TODO     | bricscad dialect reports AutoCAD's sysvars, not BricsCAD factory defaults (phase-2 harvest) |
| system-variables                              | dialect-epic    | TODO     | AutoCAD/BricsCAD sysvars: collect/specify/implement; coupling backlog, partly gated on drawing-database-api (17f) |
| autocad-no-rest-output-capture                | cad-runtime     | TODO     | no-&rest path loses probe output on AutoCAD |
| vl-catch-all-apply-optional-arglist           | divergence      | DONE     | **RÉSOLU 1.8.45, issues/closed/.** L'argument est devenu OPTIONNEL et vaut liste vide. LES DEUX questions du ticket sont tranchées, avec le raisonnement dans le ticket pour qu'on ne les rejoue pas. (1) Dialectes silencieux = bricscad (établi par le fichier vendeur) + lax + clautolisp ; AutoCAD volontairement EXCLU — sa documentation exigeant l'argument ne dit rien de ce que fait le moteur, et ce projet s'est déjà trompé ainsi (le « Sunday=1 » de vl-file-systime, un exemple qu'aucun document vendeur ne soutenait) ; la sonde probes/sources/probe-vl-catch-all-apply.lsp pose la question et la liste bougera à la réponse, pas avant. (2) --strict ACCEPTE et AVERTIT plutôt que de refuser : refuser empêcherait de charger un fichier vendeur ne serait-ce que pour le lire, et la convention du dépôt est qu'un problème de dialecte se signale, ne se lève pas. Enregistré comme :vendor-divergence, donc entrée d'annexe et contrôle de cohérence des tags. TROUVÉ EN CODANT : ma première version testait le NOM du dialecte contre :bricscad et avertissait sous --dialect bricscad — qui résout en :bricscad-v26 ; les noms sont versionnés, une famille se teste sur le PRODUIT. Attrapé en exécutant, pas en relisant. Test épinglé sur l'ARITÉ et non sur le fichier vendeur (non redistribuable, absent hors macOS : un test qui le chargerait passerait en étant sauté là où il compte). Spec 0.9.6, qui corrige au passage une dérive préexistante — l'en-tête disait 0.9.5, la prose annonçait encore 0.9.2. ANCIEN TEXTE : NOUVEAU 2026-08-16, trouvé en vérifiant macroman-encoding-name de bout en bout : une fois le défaut d'encodage levé, le vle-extension.lsp livré par BricsCAD V26 va plus loin et échoue sur l'ARITÉ. L'appel est celui du VENDEUR (offset 4452) : =(vl-catch-all-apply 'vl-load-com)=, un seul argument — BricsCAD charge ce fichier au démarrage, donc BricsCAD accepte forcément la forme à un argument, alors que clautolisp exige les deux dans TOUS les dialectes. Conséquence : aucun système ALPM référençant la bibliothèque BricsCAD installée ne charge sous =--dialect bricscad= — le dialecte qui prétend émuler ce moteur refuse le fichier du moteur. NE PAS deviner la portée : le comportement d'AutoCAD n'est PAS connu (aucune sonde), d'où un critère d'acceptation qui exige une mesure avant de choisir entre « bricscad+lax+clautolisp » et « tous les dialectes » ; --strict doit rester strict. Le test de régression doit porter sur l'arité, pas sur ce fichier vendeur (non redistribuable, absent hors macOS) |
| autocad-runtime-vlax-sleep-and-rest           | cad-runtime     | TODO     | vlax-sleep / &rest portability on AutoCAD |
| accoreconsole-deported-load-highbyte          | cad-runtime/enc | TODO     | accoreconsole (load) errors on a high-byte source file |
| accoreconsole-macos-headless-qt-blocker       | cad-runtime     | BLOCKED  | PARKED — Autodesk macOS bug (V26 W.119.M.325 + V27 X.60.M.171, even their own acad20XX.LSP fails); real AutoCAD conformance runs on Windows |
| alfe-bricscad-automation-macos                | alfe-backend    | DONE     | RÉSOLU 1.8.34 — pjb a choisi l'option A : « sur macos, les scripts ne marchent pas en combinaison avec --automation ». `--mode automation' devient WINDOWS-ONLY et signale BACKEND-NOT-AVAILABLE :NO-AUTOMATION hors Windows — le refus que Linux donnait déjà — au lieu de lancer un CAD et d'expirer à 240 s. Le refus vit dans CHOOSE-EFFECTIVE-MODE et non dans BUILD-LAUNCH-ARGV : c'est ce qui le rend IMMÉDIAT, l'émetteur et le constructeur d'argv passant tous deux par là, donc rien n'est écrit dans le workdir et aucun process n'est lancé avant l'erreur. DEUX messages distincts, car deux situations y arrivent : `--mode automation' explicite (« pas supporté sur cet OS, utilisez --mode batch ») et `--mode auto' SANS binaire (l'ancien repli sur automation disparaît, donc le message nomme le vrai problème : BricsCAD introuvable) — c'est la moitié la plus utile, un utilisateur sans BricsCAD recevait avant 240 s de silence. GARDÉS délibérément, comme le ticket le demande : l'émetteur AppleScript, MACOS-APP-BUNDLE-FOR, le préflight Accessibility et le rapport d'état du launcher — ils ont rendu les cinq tours d'investigation interprétables, et l'option B (piloter le launcher à la main devant la machine) reste ouverte ; un test vérifie que l'émetteur est toujours FBOUNDP pour qu'une passe « code mort » ne le supprime pas. 4 tests neufs écrits pour affirmer la bonne chose sur LES DEUX plateformes (sur Windows le mode se résout, ailleurs il refuse), donc non vacués sur la lane Windows. 599 checks, Fail: 0, vérifiés par NOM dans le log. issues/closed/ |
| alfe-autocad-vbscript-comments                | cad-runtime     | TODO     | CORRIGÉ EN CODE 1.8.33, mais reste OUVERT : un critère d'acceptation de pjb n'est pas rempli. Le gabarit VBScript s'ouvrait sur des commentaires `;;' (syntaxe Lisp ; VBScript veut une apostrophe ou REM) — erreur de syntaxe ligne 1, avant tout contact avec le COM AutoCAD. DEUX exemplaires du ticket ont coexisté quelques heures : celui poussé depuis Windows (message cscript réel, repro exacte, critères d'acceptation) et un plus court classé en closed/ au moment du correctif ; FUSIONNÉS en gardant le texte Windows, la copie closed/ supprimée. Critères : 1 fait (plus de `;;'), 3 fait (3 tests portant sur le FICHIER ÉMIS, dont un qui prouve que le détecteur sait échouer), 4 fait (protocole du pont intact) ; **2 NON VÉRIFIÉ** — un run réel `alfe --autocad --cad acad-2022 --mode automation' atteignant READY, qui exige le runner Windows+AutoCAD. Aucun job ne pouvait l'atteindre : les jobs CAD portaient les tags bricscad/autocad, qu'AUCUN runner ne porte (zéro pour les deux, vérifié par API) — corrigé dans la même livraison via le tag unique `cad'. En marge : BricsCAD portait le MÊME défaut au même endroit (en-tête copié, jamais signalé car son chemin automation Windows n'a pas été exercé), plus un tiret cadratin alors que le fichier est écrit UTF-8 sans BOM et lu en ANSI par cscript. Prochaine étape = programmer les sondes AutoCAD sur le runner `cad' désormais atteignable |
| dialect-platform-version-axis                 | dialect-design  | TODO     | autocad vs autocad-mac + version suffixes; drives "/…"-warns-on-windows |
| clautolisp-dialect-portability-warnings       | dialect         | DONE     | RÉSOLU 1.8.31 — pjb a tranché le format le 2026-08-14 : on garde le TAG comme identifiant stable, on rapporte fichier/ligne/fonction (on n'avertit qu'une fois par occurrence, donc on peut payer la recherche), le texte reste libre et tient sur UNE ligne, et un REGISTRE indexé par [tag] porte le détail des divergences + les références, destiné à devenir une annexe du manuel. Le blocage reposait sur une fausse contrainte : le ticket disait « les tests épinglent le message », or ils n'épinglent que le TAG — d'où zéro assertion existante réécrite. Ligne = `[TAG] FICHIER:LIGNE in FONCTION: une ligne courte'. La fonction vient de la pile d'appel vivante, sauf DEFUN qui passe le nom qu'il définit (son corps ne tourne pas encore). Fichier:ligne vient de la source-map du débogueur, absente en run de production → dégradation propre, jamais de position inventée. Le conseil « --dialect X to silence » quitte la ligne pour l'annexe. **Le registre couvre 18 tags, pas 4** : la liste grepée par pjb contenait 2 faux positifs ([clock-cache] = un defstruct de cador ; [dcl-debug] = trace de mise au point) et [enc-CODE] qui n'est pas un tag mais le gabarit d'une docstring — toute la famille enc-* construit son crochet depuis un mot-clé, donc ces tags n'existent NULLE PART en littéral et un grep ne peut pas les énumérer ; 3 vrais tags manquaient (enc-invalid-value, sec-untrusted-load, sec-untrusted-open). D'où UNREGISTERED-DIAGNOSTIC-CODES qui parcourt les jeux de codes fermés + un test qui échoue si on ajoute un code sans entrée. 5 familles (:portability 4, :extension-notice 2, :vendor-divergence 2, :encoding 8, :trust 2). Annexe documentation/clautolisp-dialect-warnings.org GÉNÉRÉE du registre, suivie en git, avec `make check-dialect-warnings-annex' câblé dans la lane documentation GitLab — contrôle vérifié comme sachant ÉCHOUER. 6 tests neufs, SBCL+CCL exit 0. issues/closed/ |
| cad-path-dotdot-resolution                    | divergence      | DONE     | RESOLVED 1.7.5 (warn on any ".." outside --lax, UP everywhere); probe already ran on granted BricsCAD runners (stale "needs bricscad-access" label); pjb decision shipped; deferred BACK-on-Windows alternative rehomed to dialect-platform-version-axis; issues/closed/ |
| bricscad-undocumented-clisms                  | dialect-invent  | BLOCKED  | bricsys-support — external vendor confirmation is ALL that remains; nothing in the tree waits on it. HARVESTED 2026-08-07 on real BricsCAD: setf returns a constant sentinel 2 and does NOT assign (not a setq alias — don't imitate); let = real parallel binding; let* does NOT exist (refuted the assumption); &rest works, bare & rejected. Spec page + `let' shipped 1.8.9; the last implementable item — the [ext-bricscad-undocumented] note for setf/let* via a recognise-but-don't-define hook in resolve-unbound-function-or-signal — shipped 1.8.26. Both stay :undefined-function; the note only explains the refusal, and is deliberately NOT dialect-gated (a --dialect bricscad user is the likeliest to be surprised, since on the real engine setf exists) |
| clautolisp-windows-pathname-mapping           | portability     | DONE     | **RÉSOLU 1.8.38, fusionné en MR !81 le 2026-08-15 — issues/closed/.** W0..W4 livrés, W5 hors périmètre par le §7 du ticket lui-même (les logical pathnames y sont un NON-objectif) : on ferme sur le périmètre que l'issue s'était fixé, pas sur un périmètre rogné. Historique conservé ci-dessous. needs windows-access; design spec written — **BLOCAGE LEVÉ 2026-08-15** : le runner Windows est en ligne et sain (test:clautolisp:windows, test:alfe:windows, build:alfe:windows, conformance:pathname-mapping:windows tous verts). La spec de conception est écrite ; plus rien n'attend un accès. Candidat immédiat **AVANCÉ 1.8.38.** W4 CONFIRMÉ sur du vrai Windows : le job conformance:pathname-mapping:windows que le ticket disait « à câbler » l'était déjà, et il TOURNE depuis que le runner est revenu et que le tag `cad' a laissé passer le pipeline enfant. Sortie : toute la matrice synthétique (build × run) verte PLUS un aller-retour sur un vrai fichier, avec `build=MSYS2 run=MSYS2' DÉTECTÉ — pas un repli sur :posix, donc la classification du §5 marche sur la machine réelle. Le « Still TODO » est fait aussi : les tables de montage sont désormais SEMÉES DU SYSTÈME VIVANT (/etc/fstab pour cygwin/msys2, `mount' pour WSL) et préférées aux conventions, celles-ci restant dessous pour ce qu'elles ne mentionnent pas. Parsing séparé de l'acquisition pour être testable sur un hôte qui n'a ni l'un ni l'autre ; LIVE-MOUNT-ENTRIES ne signale jamais (pas de fstab, pas de `mount' → on retombe sur les conventions, la couche doit démarrer). Détails traités, chacun testé : ordre des champs fstab, pseudo-systèmes (none/cygdrive/proc) écartés, échappement \040 des espaces (indispensable sous « Program Files »), et sous WSL 9p ET drvfs gardés mais pas ext4/proc. 7 tests neufs, SBCL+CCL exit 0. RESTE : W5 (logical pathnames) hors périmètre par le §7 |
| deferred-clautolisp-out-of-dialect-warnings   | dialect         | DEFERRED | availability bitmap |
| deferred-document-lifecycle-command-semantics | cad-semantics   | DEFERRED | pjb-decision (NEW/OPEN/SAVE/CLOSE vs running routine) |
| deferred-bricscad-setq-nil-returns-2          | dialect         | DONE     | RESOLVED 2026-08-07: the "2" is decoded — BricsCAD setf returns a constant internal sentinel 2 and does NOT assign (harvest run). clautolisp won't imitate it. issues/closed/ |
| vla-accessor-family                           | cad-runtime     | TODO     | CI 2026-08-10 : job test:cad:schms:linux ajouté sur poseidon (headless, host=cador — aucun CAD requis), INERTE tant que SCHMS_REPO_URL n'est pas défini (corpus client privé sncf-reseau/src/schms). REPRIORITISED P1 2026-08-07 (was absent from the rebuilt index): SCHMS =make test-cad-clautolisp= fails 6/6 [sigfic-cad] with "VLA-OBJECT AutoCAD.Document has no property named BLOCKS". Collection layer RESOLVED 1.8.10 same day (live drawing-backed Blocks/Layers/ModelSpace/PaperSpace + generic Item/Count/Add/Delete, verified end-to-end on mock). Remaining split into vla-entity-property-bridge below; keep NEXT until the SCHMS re-run confirms what it consumes |
| vla-entity-property-bridge                    | cad-runtime     | TODO     | NEW 2026-08-07, split from vla-accessor-family; *LANDED same day 1.8.11* for the SCHMS subset (scalar + point properties via runtime wrap/unwrap hooks — no struct migration; Alignment enum; Rotation stays radians per the entget convention). Same release: entmake BLOCK/ENDBLK block definitions (fixes the "Item: no item named sigfic_N" round) + main-space filtering of ssget X / entlast / entnext + (entnext (tblobjname "BLOCK" n)) walk. Third round ("no method named INSERTBLOCK") RESOLVED 1.8.12: InsertBlock + attribute instantiation + GetAttributes/HasAttributes + Move/Rotate/Copy/Delete/Erase/Update + GetBoundingBox (by-reference) + vlax-invoke/vlax-get/vlax-put. Ten SCHMS rounds later, *CONFIRMED RESOLVED 2026-08-07*: =make test-cad-clautolisp= (host=cador) 6/6 OK at 1.8.20. The journey shipped 1.8.10–1.8.20: live collections, entmake BLOCK/ENDBLK, InsertBlock+attributes, entity property/method bridge, vlax-invoke/get/put, BLOCK -2 walk, the drawing-command engine (LINE/CIRCLE/TEXT/DONUT/SOLID/ERASE/MOVE/COPY/ROTATE/-BLOCK + selection sets through COMMAND), every-point-group transforms, justification-aware text boxes. Remaining here = SPEC-UNCERTAIN vendor probes + CCL lane verify |
| cador-1-split-nihil-cador                     | infra           | DONE     | name the two headless hosts: nihil=stub host (today null-host), cador=real CAD core (today mock-host); rename + nihil warn-stubs + --host aliases. Epic sub-task 1, implement now |
| cador-2-multidocument-runtime                 | feature         | TODO     | N isolated document namespaces/engines/programs, shared blackboard on one heap (no copy), per-doc drawing; cooperative coroutine scheduler (1 thread/doc + 1 aldo/doc). Epic sub-task 2, parallel branch cador-multidocument |
| autolisp-spec-alpm-verification-findings      | spec            | DONE     | RESOLVED 2026-08-09 (spec 0.9.3 → 0.9.5). All 8 per-entry updates + the ch.22 additions shipped; 21 entries moved from "presumed compatible pending Phase 4" to verified on both vendors. New ch.22 sections: VLX namespace family verification, DEScoder command line, .prj/.prv formats; VLISP-COMPILE mode-mapping table; a normative rule that namespace isolation is a delivery facility, not a correctness/security boundary. DEFUN-Q-LIST-SET deliberately left "presumed" — probe V-11 never touched it. issues/closed/ |
| vl-doc-namespace-family-bricscad              | doc             | DONE     | RESOLVED 2026-08-09, docs-only as triaged (no code change: the 13 builtins were already ungated). Fixed the multidocument analysis in FOUR places, not the two first spotted — the scope-table row, R8, plus two further stale claims traced to one 2017 blog post predating BricsCAD V18.2: "BricsCAD has no separate-namespace compilation mode" (refuted — DESCoder -namespace is one) and experiment E9, which was framed to confirm that mode's absence. issues/closed/ |
| vl-file-systime-parity                        | divergence      | DONE     | RESOLVED 1.8.23 / spec 0.9.3. The documentation scan overturned the premise: the "Sunday=1" Autodesk example was a bad datum (2003-09-10 is a Wednesday = 3, not 4), and NO vendor example falls on a Sunday — so Mon..Sat = 1..6 is all the docs establish and the divergence is one value wide (AutoCAD 0 = POSIX, BricsCAD 7 = ISO). Shipped: 8 elements everywhere, dialect-aware Sunday, ms seam returning 0, 56-check regression test, rewritten spec entry with a *** clautolisp note. Residual: probe more versions with a forced mtime — folded into vendor-probe-autocad-bricscad-divergences. issues/closed/ |

* P2 — language / builtins completeness
  :PROPERTIES:
  :Priority: P2
  :END:

| Issue                                    | Type          | Status   | Note |
|------------------------------------------+---------------+----------+------|
| pi-constant-unbound                      | builtin       | DONE     | PI evaluates to NIL in every dialect (vendors: 3.14159…); one-line fix, 97 uses in the SCHMS+ sources; error surfaces at the arithmetic, not at the constant — RESOLVED 1.7.25 (bind PI/T/PAUSE), issues/closed/ |
| complete-unit-tests                      | coverage-epic | TODO     | backfill builtin coverage to ≥95% |
| deferred-stubbed-functions               | builtins-epic | DEFERRED | promote stubbed-as-no-op builtins (registry section done 1.5.1) |
| deferred-spec-research                   | builtins      | DEFERRED | vendor-validate uncertain builtins; needs cad-access |
| xdroom-xdsize-not-implemented            | builtin/draw  | DEFERRED | XDROOM/XDSIZE still nil stubs (schms-data-selection) |
| cador-3-host-interface-protocol-spec     | spec          | TODO     | D1 host-interface spec + formal AutoLISP↔host protocol + D2 cador spec + D5 naming note. Epic sub-task 3 |

* P3 — everything else (by workstream)
  :PROPERTIES:
  :Priority: P3
  :END:

** Drawing / entity parity (SCHMS corpus)
Headless cores largely DONE; most families parked; CAD verification is
the shared blocker.

| Issue                              | Status   | Note |
|------------------------------------+----------+------|
| ssget-implied-selection-not-graphical | DONE   | "_I" est classe avec les modes graphiques alors qu'il n'en est pas un : il rend la selection courante, sans pointeur ni geometrie, et le stockage pickfirst dont il depend est deja modelise. Dernier blocage du corpus CAD SCHMS+ en headless — RESOLU 1.8.2 (ssget "_I" pickfirst, filtre partage, PICKFIRST, consomme a la lecture), issues/closed/ |
| entity-mutation-parity             | TODO     | headless foundation done (1.6.5/6); umbrella for deferred families + CAD verify |
| drawing-database-api               | TODO     | parent epic — Phase 17f multi-document / 17g ObjectDBX / 17h |
| drawing-data-structures-parity     | BLOCKED  | headless done (1.6.8–10); needs cad-access to verify — **RÉEXAMINER 2026-08-15** : le blocage `cad-access' est levé, le runner porte le tag `cad' et les sondes vendeur ont tourné sur AutoCAD ET BricsCAD réels le 14/08 (drawing-data 27 conforms sur autocad / 26 + 1 known-divergence sur bricscad ; selection 21/21 sur les deux ; exit 0 partout). Vérifier si la « verification » que cette ligne attend est justement celle-là — auquel cas elle est DONE — ou s'il reste des observations hors du corpus des sondes |
| selection-and-snapshot-parity      | BLOCKED  | headless done (1.6.8–10); needs cad-access to verify — **RÉEXAMINER 2026-08-15** : le blocage `cad-access' est levé, le runner porte le tag `cad' et les sondes vendeur ont tourné sur AutoCAD ET BricsCAD réels le 14/08 (drawing-data 27 conforms sur autocad / 26 + 1 known-divergence sur bricscad ; selection 21/21 sur les deux ; exit 0 partout). Vérifier si la « verification » que cette ligne attend est justement celle-là — auquel cas elle est DONE — ou s'il reste des observations hors du corpus des sondes |
| entnext-nongraphical-enumeration   | BLOCKED  | verification blocked on libredwg-upstream |
| libredwg-empty-dwg-write-invalid   | BLOCKED  | libredwg-upstream bug (minimal repro filed) |
| entmake-symbol-table-records       | DEFERRED | table-record creation from AutoLISP |
| entity-family-annotation           | DEFERRED | schms-parity 1.6.6 |
| entity-family-hatch-image-boundary | DEFERRED | schms-parity 1.6.6 |
| entity-family-3d-solids            | DEFERRED | schms-parity 1.6.6 |
| entity-family-tables-viewport-misc | DEFERRED | schms-parity 1.6.6 |
| entity-family-nongraphical-objects | DEFERRED | schms-parity 1.6.6 |

** Encoding (pjb-driven)
| Issue                            | Status  | Note |
|----------------------------------+---------+------|
| encoding-situations-cli-options  | TODO | **BLOCAGE PÉRIMÉ, corrigé 2026-08-15.** La ligne disait « blocked on pjb validating the situation list (§3) » ; or le ticket lui-même porte en tête « *VALIDATED 2026-07-28 (pjb)* : liste arrêtée à SIX (source, file, console, cadstdio, log, terminal), points d'arbitrage a–d résolus, -e et -E ABANDONNÉS, et « implementation of the settled situations + CLI can start ». Il n'attend donc plus pjb. Restent en aval : la table des défauts du §7 (expériences E1–E5) et le plumbing du §6. E2 et E4 sont DONE ; E1 (AutoCAD LISPSYS × BOM) et E3 (console BricsCAD en batch -B) exigent le runner CAD — désormais atteignable, cf. le tag `cad'. PIÈGE E1 consigné dans le ticket : LISPSYS est au niveau REDÉMARRAGE et alfe passe /isolate qui l'épingle à 0, donc la campagne doit relancer le CAD par niveau ET retirer /isolate, sinon elle mesure trois fois la même chose. Débloque aussi encoding-consolidation |
| encoding-consolidation           | BLOCKED | umbrella program/PLAN; gated behind the situation list above |
| macroman-encoding-name           | DONE | NOUVEAU+RÉSOLU 1.8.44 (rapporté par pjb 2026-08-16, ALPM chargeant vle-extension.lsp de BricsCAD V26 : « Undefined external-format: :MACROMAN »). Même classe que cp1252-codec-host-independent, mais le trou est un NOM, pas un codec : SBCL enregistre MAC-ROMAN et MACINTOSH, PAS MACROMAN ; CCL enregistre les trois. Et babel ne livre AUCUNE table Mac Roman (jeu vérifié : ascii, cp125x, cp437, cp932, ebcdic, gbk, euc-jp, iso-2022-jp, iso-8859-*, koi8-*, ksc-5601, unicode), donc le repli single-octet — dont les tables VIENNENT de babel — ne peut pas boucher ce trou : normaliser le nom est la SEULE réparation. C'est le squeeze de NORMALIZE-ENCODING-NAME (retrait des tirets/underscores AVANT intern) qui CRÉAIT le bug : mac-roman, MAC_ROMAN et macroman tombaient tous sur l'unique orthographe que SBCL n'a pas — seul "macintosh" marchait. Pas une orthographe de niche : BricsCAD/macOS rend (getvar "SYSCODEPAGE") = "MAC_ROMAN", donc l'idiome portable (setq *AUTOLISP-FILE-ENCODING* (getvar "SYSCODEPAGE")) était cassé sur les builds SBCL et bon sur les builds CCL. Table d'alias unique dans autolisp-runtime, consultée par les TROIS sites (leçon déjà payée en 1.8.28 : corriger OPEN seul aurait laissé LOAD et le CLI cassés) — LOAD, OPEN (avant la branche CP-NNNN, sinon CP-10000 partait sur :CP10000), et -e/-E. Délibérément PAS dans *ENCODING-ALIASES* (les quatre obligatoires) : mac-roman n'est ni obligatoire ni rattrapable. Chaque suite épingle que le mot-clé émis passe EXTERNAL-FORMAT-AVAILABLE-P sur l'hôte courant. RESTE (hors dépôt) : ALPM canonisait vers "macroman" — un nom qu'aucun Common Lisp ne connaît, hérité du codec Python mac_roman — corrigé en "mac-roman" dans alpm/sources/alpm.lsp, le générateur et les .alpm générés |

** CI / build / test infra
| Issue                         | Status | Note |
|-------------------------------+--------+------|
| ci-github-ccl-lane            | DONE   | VÉRIFIÉ 2026-08-10 : déjà livré, jamais fermé. .github/workflows/ci.yml porte le job `ccl-test' (clautolisp test-ccl + file-compat-ccl + alfe test-ccl) via l'action composite setup-lisp `ccl: true'; triggers, préambule et additivité conformes aux critères. issues/closed/ |
| windows-runner-quicklisp-deps | DONE   | RÉSOLU 2026-08-14 en MESURANT plutôt qu'en provisionnant. pjb a remis le runner Windows en ligne ; un pipeline master neuf (2761319717) a suffi pour que detect:runners le voie et que le pipeline enfant natif crée ses jobs. Résultat : test:alfe:windows, test:autolisp-test:windows, build:alfe:windows et conformance:pathname-mapping:windows TOUS VERTS ; aucun « Unable to process HTTP header » nulle part, et les traces montrent babel chargé depuis la dist locale du runner — l'état « deps présentes, zéro téléchargement en job » que le ticket demandait. babel et trivial-gray-streams (deps cœur seulement depuis 1.8.28, donc APRÈS le dépôt du ticket) se résolvent aussi. test:clautolisp:windows échoue encore mais PAS là-dessus : sa trace montre toutes les suites à Fail: 0, puis le job meurt dans verify-vl-registry.lisp qui suit make test-sbcl — bug indépendant, sorti en vl-registry-reg-exe-oem-codepage.issue et corrigé en 1.8.32. La suite unitaire clautolisp est donc VERTE sur Windows. RIEN à provisionner : ni bundle ni miroir local, le travail que la Proposal décrivait aurait été perdu. Mesurer d'abord a coûté un pipeline. issues/closed/ |
| vl-registry-reg-exe-oem-codepage | DONE | NOUVEAU+CORRIGÉ 1.8.32 : reg.exe répond dans la CODEPAGE OEM de la console (CP850 en Europe de l'Ouest, CP437 aux US), pas en UTF-8 — et décoder ça en UTF-8 ne produit pas du mojibake, ça SIGNALE (« l'octet #(130) ne peut pas être décodé »). L'octet 130 est le é de « Opération terminée avec succès », le message de SUCCÈS de reg.exe : l'opération avait marché, c'est lire la confirmation qui tuait. Tuait DEUX jobs (verify:vl-registry:windows et, après une suite entièrement verte, test:clautolisp:windows). %REG-WRITE mourait en décodant une sortie qu'il jette. Jamais vu avant parce que les sondes tournaient sur Windows anglophone, où ces messages sont en ASCII pur. Correctif : :external-format explicite, codepage console lue via chcp (dernière suite de chiffres — chcp répond dans une phrase localisée), SBCL connaît :cp850/:cp437 nativement (mesuré : c'est babel qui n'a pas CP850), repli :latin-1 transparent aux octets qui ne peut pas signaler. Test de plateforme à l'EXÉCUTION et non #+windows, pour que la branche Windows soit compilée partout. 5 tests neufs. issues/closed/ |
| runner-availability-gating    | DONE   | auto-start native macOS/Windows jobs when their intermittent runner is ONLINE, manual fallback when offline; fail-safe (sans RUNNER_STATUS_TOKEN tout reste manual). Orchestrator-play (2026-08-07) ABANDONNE — un job when:manual n'est jouable qu'une fois son stage actif et ses needs satisfaits, donc une passe unique en .pre ne peut pas demarrer une cascade ; remplace par le pipeline enfant .gitlab/native.yml dont les rules sont evaluees a la creation de l'ENFANT, quand MACOS_RUNNER/WINDOWS_RUNNER sont connus. Blocage sur le token perime depuis le 2026-08-15. FERME 2026-08-20 sur OBSERVATION, pas sur lecture du YAML : la pipeline master 2777241866 (18:58Z) est tombee sur un runner en ligne ET un runner hors ligne EN MEME TEMPS, ce qui est exactement le cas que les criteres demandent de distinguer — detect:runners a publie MACOS_RUNNER=online / WINDOWS_RUNNER=offline, et dans l'enfant les jobs macOS sont partis SEULS (pending) pendant que les trois sondes Windows restaient manual, sans un seul stuck_pending. Les trois criteres d'acceptation sont donc satisfaits, chacun par une mesure. APPRIS EN PLUS, et hors criteres : la porte ne protege que les pipelines CREEES pendant que le runner est hors ligne — un portable qui s'eteint PENDANT un job donne autre chose (pipeline 2776267840 : la sonde bricscad:windows est morte en server_timeout_running a 1800 s pile avec une trace VIDE, et les deux sondes AutoCAD derriere elle en stuck_pending_no_matching_runners, le runner etant a concurrence 1) ; ce volet appartient a cad-runner-wedged-by-modal-dialog |
| cad-runner-wedged-by-modal-dialog | TODO   | NOUVEAU 2026-08-14 : un job CAD figé bloque TOUS les autres. verify:bricscad-hidden-ui:windows a lancé BricsCAD sans dessin ni profil → dialogue modal invisible sous /Automation → le timeout 180 s d'alfe a bien tiré, mais le PROCESSUS CAD est resté vivant, donc le JOB n'a jamais fini : 32 minutes de détention du runner `cad' à concurrency=1, 70 jobs CAD empilés sur neuf pipelines, une heure de batterie perdue et AUCUN job exécuté. Sans `timeout:' le plafond était le défaut projet (1 h). MITIGÉ ici : timeout: 30m sur les quatre gabarits CAD — valeur MESURÉE (plus long job légitime = encoding:experiment:bricscad:windows à 1158 s ; tout le reste < 223 s), donc ~55 % de marge et pire cas divisé par deux. RESTE OUVERT : alfe devrait tuer le moteur qu'il a lancé quand l'attente du protocole expire (les drivers .ps1 le font déjà en filet de sécurité, preuve que le problème était connu mais traité au mauvais niveau), et un blocage par dialogue mérite un diagnostic distinct de « démarrage lent » — **MOITIÉ MÉCANISME RÉSOLUE 1.8.39, fusionnée en MR !82.** La prémisse était FAUSSE et le dire compte plus que le correctif : BricsCAD reapait DÉJÀ son moteur (unwind-protect autour de l'attente READY). Ce qui manquait était sur l'AUTRE backend — AutoCAD n'avait aucune garde au timeout READY, et son shutdown faisait `(uiop:wait-process)' NON BORNÉ, précisément l'appel que le tueur de BricsCAD existe pour éviter (sa docstring le dit). Défaut structurel derrière les deux symptômes : le tueur soigneux était PRIVÉ d'un seul backend ; KILL-ENGINE-PROCESS vit désormais dans backend-cad-common. 4 tests sans CAD, dont un processus qui PIÈGE SIGTERM (un test sur un processus coopératif ne prouverait pas la bornitude) et un qui affirme sur le CODE, commentaires retirés. **RESTE : la moitié DIAGNOSTIC seulement** — « READY-TIMEOUT, last status BOOTING, launcher still running » se lit pareil pour un démarrage lent et pour un dialogue modal ; les distinguer aurait désigné la cause en une lecture au lieu de trois hypothèses fausses. C'est un MESSAGE, pas un mécanisme, et le mécanisme est désormais sûr |
| build-tool-paths-unquoted     | DONE   | RÉOUVERTE : le volet quotage est bien résolu en 1.8.27 (105 sites, 7 Makefiles, 4 sondes `command -v`), mais une SECONDE forme y survit — build-libredwg.sh:115 passe `-DCMAKE_C_COMPILER="$compiler_path"`, et CMake tronque au `:` de « C:/… », d'où « The CMAKE_C_COMPILER: C is not a full path ». Quoter le shell n'y suffit pas ; il faut `:FILEPATH=` ou `cygpath -u`. Bloque build:clautolisp:windows, sans contournement possible côté appelant — CORRECTION DE CAUSE 2026-08-14 (pjb) : le volet CMake de ce ticket accuse à tort une troncature de `C:/' par CMake — « ce n'est pas une troncation de c:/ qui est assez bien gere par cmake, mais il y avait un probleme pour retrouver CC parmis les differents environnemnt windows ». Le correctif -DCMAKE_C_COMPILER:FILEPATH que cette analyse motivait est donc à écarter : il répare un problème inexistant. Le vrai sujet est la SÉLECTION du compilateur parmi les sous-systèmes MSYS2 (MSYS/MINGW64/UCRT64/CLANG64) — ce que la section « Chemin parcouru » décrivait déjà sans en tirer la conclusion. Le second défaut TIENT : le garde-fou `case ... /ucrt64/*' ne reconnaît que la forme POSIX, donc il est silencieusement inopérant dès que CC arrive en forme Windows. Détail consigné dans le ticket — **MÉCANISME TROUVÉ 2026-08-16** via run-forest-run (poseidon:/home/pjb/src/fabrik, désigné par pjb) : la correction de cause disait OÙ chercher sans dire COMMENT `CC' finissait en forme Windows. Réponse : la couche MSYS **RÉÉCRIT la valeur à l'entrée du processus**, donc `/ucrt64/bin/gcc' posé depuis PowerShell ressort en `C:/msys64/ucrt64/bin/gcc'. D'un seul coup s'expliquent le job qui affiche pourtant `CC=/ucrt64/bin/gcc' à son dump (les deux sont vrais, à deux endroits de la chaîne) ET le garde-fou `case ... /ucrt64/*' inopérant — il teste une forme que la conversion a déjà détruite ; le « second défaut indépendant » est donc le MÊME problème vu en aval. Correctif appliqué : poser CC côté BASH en forme POSIX, MSYS2_ENV_CONV_EXCL=CC, MSYS2_PATH_TYPE=inherit avant `bash -l' (sinon le profil remplace le PATH Windows et sbcl disparaît), MINGW64 en tête quel que soit MSYSTEM. DÉSACCORD SIGNALÉ, pas masqué : la correction du 14/08 dit que CMake gère bien `C:/', le commentaire de run-forest-run attribue la troncature à CMake découpant sur `:' — les deux sont de pjb ; le correctif ne dépend pas de l'arbitrage, garder la forme POSIX supprime le deux-points. FAIT : clautolisp verse avec-bash.ps1 + environnement-windows.sh et sa lane release Windows les utilise. RESTE : build-libredwg.sh lui-même n'est pas corrigé, son garde-fou est toujours POSIX-seul — c'est ce qui garde ce ticket ouvert. FERME 2026-08-20 (1.8.62) en corrigeant build-libredwg.sh, ce que le ticket disait etre le seul reste. Deux trouvailles en le faisant. (1) LA CAUSE ECRITE DANS LE TICKET EST FAUSSE : il accusait CMake de traiter ':' comme separateur de liste dans un -D et proposait -DCMAKE_C_COMPILER:FILEPATH= ; reproduit sur CMake 3.31.6 avec un projet minimal qui reimprime la valeur recue, -DFOO=C:/msys64/ucrt64/bin/gcc passe INTACT, type ou non. Le decoupage vient donc d'ailleurs, tres probablement de la conversion d'arguments de MSYS2 qui voit une LISTE de chemins des qu'il y a un deux-points ; corriger sur la foi de la cause supposee aurait livre un :FILEPATH qui ne repare rien. (2) UN SECOND DEFAUT que personne n'avait vu : command -v rend ce qu'on lui donne, donc CC=C:/msys64/ucrt64/bin/gcc (ce que fait la CI de SCHMS+) ressort en forme Windows, et le garde-fou qui doit REFUSER les compilateurs UCRT64/Clang est un glob POSIX (/ucrt64/*) qui ne peut pas y correspondre — verifie. Un chemin en forme Windows passait donc devant le controle existant precisement pour arreter ce compilateur, et le build echouait plus loin sur autre chose ; c'est exactement le cas signale. CORRECTIF : normaliser en forme POSIX (cygpath -u) juste apres command -v, au SEUL point d'entree possible et seulement sous MSYS2/Cygwin — une correction, deux defauts couverts, la question de savoir qui decoupait le deux-points cessant de se poser puisqu'il n'en reste plus. Ajoute : le script imprime l'argument tel que cmake le recoit, cet argument ayant ete mal livre deux fois — un run qui l'affiche se diagnostique au lieu de couter un aller-retour vers une machine Windows. TESTE ici sur les 4 cas avec un cygpath bouchon (forme Windows UCRT64 -> garde-fou se declenche enfin ; POSIX UCRT64 -> se declenche ; POSIX MINGW64 -> accepte ; Linux -> bloc inerte), motif ?:[/\\]* verifie dans les deux sens. NON VERIFIE et dit plutot que devine : le build Windows reel de bout en bout, qui demande le runner Windows, hors ligne |
| install-sources-with-libraries | DONE  | RESOLVED 1.8.23 (option 1, pjb's call). libredwg is a submodule and ONLY clautolisp/drawing-dwg depends on it, so a missing codec no longer fails install-libraries: it installs sources + ASDF systems + the rest, warns, names the one affected system and the commands to add it later. clautolisp/Makefile `install' now runs the phase, matching the root. New REQUIRE_NATIVE_LIBRARIES (default 0) restores the hard error where an incomplete tree is a defect — release-libraries passes 1, so a published tarball can never silently ship without its codec. Verified on a disposable PREFIX in all three states + uninstall leaves no files. issues/closed/ |

| release-artefact-set-incomplete | DONE | NOUVEAU 2026-08-16 (pjb a nommé les 10 artefacts qu'une release doit publier) — **BLOQUE release-1.9.0** : couper le tag aujourd'hui publierait un jeu incomplet. VÉRIFIÉ EN EXÉCUTANT le vrai `collect-artefacts' sur un jeu d'entrées fabriqué complet, pas en lisant la recette : il sort 7 fichiers, dont 3 seulement figurent parmi les 10 demandés (documentation, sources.tar.bz2, sources.zip). TROIS causes distinctes car trois correctifs distincts. C1 : les 4 artefacts unix par plateforme (binaries/libraries × linux-x86-64/darwin-arm64) sont FUSIONNÉS en un seul couple multi-plateforme et ne sont jamais repassés dans le jeu final — ils existent comme artefacts de JOB, pas dans la release ; la fusion elle-même est correcte (vérifié que l'union est bien une union, chaque marqueur survit). C2 : `release:windows:x86-64' est un STUB qui n'écrit qu'un .txt, donc les 2 fichiers windows ne peuvent pas exister (le passthrough `*windows*' les attend, il n'y a rien à passer) ; même stub côté GitHub. C3 : `clautolisp-<ver>-all.tar.bz2' n'existe NULLE PART — zéro occurrence de all.tar.bz2 dans le Makefile, .gitlab-ci.yml, scripts/ et .github/ ; jamais implémenté, pas mal câblé. EN MARGE : `release:linux:arm32' est construit puis SILENCIEUSEMENT JETÉ (absent du `needs:' de collect:release) — le pipeline paye un build dont la sortie est perdue sans un mot ; et le job collect de GitHub est un TODO explicite qui ne fusionne rien, donc les deux forges publient des jeux DIFFÉRENTS du même tag. **TRANCHÉ ET LIVRÉ 2026-08-16** (MR !88) : pjb a choisi l'UNION DÉPAQUETÉE, plus l'ajout de linux-arm32 « et tout autre binaire que nous pourrions builder à l'avenir », plus Windows en `.zip' et un `-all' dans LES DEUX conteneurs (.tar.bz2 et .zip). C1 : les artefacts par cible repassent désormais dans le jeu final PAR GLOB, donc un nouveau runner s'y ajoute sans toucher au Makefile ; l'ancien cas spécial `*windows*' disparaît (il recopiait aussi le .txt du stub dans la release). C3 : `-all' est l'union dépaquetée de tout, sources comprises (elles s'extraient sous leur propre préfixe clautolisp-<ver>/, donc à CÔTÉ de bin/lib/share et non dedans) ; sources.zip volontairement exclu, même contenu que le tarball. arm32 ajouté au `needs:'. C2 : la lane Windows n'est plus un stub — elle joue `make release' depuis un bash MSYS2, avec le relais PowerShell→MSYS2 repris de run-forest-run ; RODAGE À FAIRE sur la vraie machine. Jeu attendu = 17 artefacts. DEUX contrôles neufs, prouvés capables d'échouer avant d'être crus (casser le passthrough nomme les 10 fichiers perdus ; renommer le -all.zip signale nom inattendu ET absence), câblés en `make check-release' et dans un job `check:release' délibérément HORS de la lane documentation, celle-ci étant sautée sur les tags — c'est-à-dire exactement le pipeline d'une release. FAIT 2026-08-19 (1.8.53) : les trois causes sont closes. C1/C3 et arm32 depuis le 16-08 ; C2 (lane Windows-bouchon), le dernier point resté ouvert, l'est aussi — release:windows:x86-64 empaquette pour de vrai via scripts/release-windows.sh, et release-1.8.49 a publié binaries/libraries-windows-x86-64.zip, téléchargés et ouverts. L'ensemble a bougé depuis la rédaction : Windows livre du .zip et non du .tar.bz2 (pjb 16-08). release-1.8.49 = 15 artefacts ; armv7 absente pour une cause distincte et documentée (SBCL armhf sans threads). check-release-artefact-set.sh est le contrôle que le critère 2 demandait : il fait tourner la VRAIE recette sur un jeu d'entrées fabriqué et diffe la liste produite (18 entrées). DERNIER CONSTAT TRAITÉ — la divergence GitHub était encore ACCIDENTELLE et VIVE : .github/workflows/release.yml se déclenchait sur chaque tag release-* et attachait à une release GitHub du MÊME NOM un ensemble sans union ni paquet Windows ; pire que ne rien publier, puisque rien ne disait au téléchargeur que son ensemble était court. Déclencheur automatique et étape « attach to release » retirés (workflow_dispatch seul, permissions ramenées à contents: read), en-tête disant pourquoi et ce qu'il faudrait fermer pour le rétablir |
| ci-native-child-pipeline-creation-fails | TODO | NOUVEAU 2026-08-19, P1 : depuis ~16:49 UTC, TOUTE pipeline — MR comme master — finit ROUGE alors qu'AUCUN job n'a échoué (15 réussis, 0 échec, 15 sautés). Le rouge vient du seul bridge native:pipeline : failure_reason downstream_pipeline_creation_failed, enfant créé et mort dans la même seconde avec ZÉRO job. Conséquence concrète : master rouge rend rouges toutes les MR suivantes, et glab mr merge REFUSE de fusionner sur pipeline rouge (le projet l'autorise pourtant — only_allow_merge_if_pipeline_succeeds est false — donc l'API passe encore, mais le chemin normal est bloqué). ÉCARTÉ, vérifié : le contenu de la branche (MR !107 ne change que deux blocs script, une variable de Makefile, un tampon de version et deux fichiers de doc) ; la config enfant (.gitlab/native.yml identique à master au bit près, et POST /ci/lint répond valid:true sans erreur ni avertissement) ; la config parente (glab ci lint passe) ; la disponibilité des runners (detect:runners rapportait macOS ET Windows online dans la pipeline même dont l'enfant a échoué, et les règles du gate finissent par when: manual, donc un runner absent donne des jobs manuels, jamais zéro job) ; le caractère transitoire (bridge relancé → deuxième enfant vide ; pipeline entièrement recréée depuis un commit neuf → troisième) ; et la fusion elle-même (master 2773471716 / 54b44c6 AVANT la fenêtre réussit, 2773597223 / 90fd7ed APRÈS échoue pareil, et rien entre les deux ne touche au pipeline natif). NON ÉTABLI : la raison exacte du refus. failure_reason est le générique downstream_pipeline_creation_failed ; le message précis n'est visible que sur la PAGE DU JOB BRIDGE DANS L'INTERFACE WEB et n'est pas exposé par l'API (GET /jobs/<bridge>/trace répond 404 pour un bridge). CAUSE TROUVEE (pjb a lu le message sur la page du job bridge) : « Project exceeded the allowed number of jobs in active pipelines. Retry later. » C'est une LIMITE, et c'est nous qui la remplissons. Les pipelines enfants ne se terminent JAMAIS, par conception — le commentaire du parent le dit (« pas de strategy: depend : quand le runner est offline les jobs de l'enfant sont manual et l'enfant ne complete jamais »). Ce qu'il n'anticipait pas, c'est qu'un enfant qui ne complete jamais reste ACTIF indefiniment et que ses jobs continuent de compter dans le plafond jobs-in-active-pipelines du projet : chaque push ajoute un enfant immortel. Mesure au moment ou la limite a mordu : 17 pipelines enfants en running, le plus ancien cree a 10:08 et toujours running a 17:50. Si c'est sorti AUJOURD'HUI c'est le volume : quatre MR, chacune re-pipelinee plusieurs fois par des rebases. Cela explique aussi pourquoi les indices semblaient contradictoires — meme config verte avant ~16:49 et rouge apres, sur master comme sur les MR : ce n'etait pas le contenu, c'etait un compteur qui se remplissait. DEBLOQUE ~17:50 : 15 enfants fantomes annules (tous sauf celui de la MR en cours de test), bridge de master relance, enfant cree, master 2773597223 repasse SUCCESS. C'est un coup de balai, pas une correction : les prochains pushes rempliront de nouveau le plafond. CORRECTIF A TRANCHER, 4 options dans le ticket ; recommandation = option 1 (auto-cancel redundant pipelines + interruptible: true sur les jobs de l'enfant : un nouveau push annule alors l'enfant supersede, changement minimal, garde le fallback cliquable, auto-nettoyant pour toute ref repoussee) complete eventuellement par l'option 2 (balayage planifie) pour les refs poussees une fois puis abandonnees, que l'option 1 ne couvre pas. OPTION 1 FAITE 2026-08-19 (1.8.56, pjb « Implemente l'option 1 ») AVEC UNE SURPRISE : le reglage projet auto_cancel_pending_pipelines etait DEJA enabled, et l'a ete toute la journee pendant que les 17 enfants s'accumulaient — donc « activer l'auto-cancel » n'aurait rien change. Ce qui manquait est l'autre moitie, et elle explique l'inertie du reglage : GitLab n'auto-annule un pipeline supersede que si TOUS ses jobs actifs sont interruptibles, et un seul job sans le mot-cle epingle tout le pipeline. Trois des vingt-six jobs de l'enfant declaraient interruptible: true individuellement — exactement la demi-mesure qui n'obtient rien, les vingt-trois autres tenant le pipeline ouvert. .gitlab/native.yml le declare desormais UNE fois pour tous via un bloc default:. Les deux configs revalidees (POST /ci/lint pour l'enfant en autonome, glab ci lint pour le parent). Sans effet sur un tag de release : l'auto-cancel ne se declenche que si un pipeline PLUS RECENT supersede un pipeline de la MEME ref, et un tag de release n'est pas repousse. NON COUVERT et dit : une ref poussee une fois puis abandonnee n'a rien qui la supersede, son enfant vit donc toujours indefiniment — c'est le trou que l'option 2 comblerait, non implementee. VERIFIABLE seulement en observant : les prochains pushes sur une branche doivent laisser UN seul enfant actif par ref au lieu d'un par push, via GET /projects/:id/pipelines?source=parent_pipeline — la requete qui compte, et celle qui manquait au premier diagnostic, le listing ordinaire EXCLUANT les pipelines enfants (d'ou le « 0 pipeline en cours » rapporte pendant que dix-sept enfants tournaient). MR !107 a été fusionnée par l'API pour cette raison, ses 15 jobs réels au vert — noté ici pour que la décision soit visible et non enterrée dans un commit de merge. OPTION 2 FAITE 2026-08-20 (1.8.58) : scripts/cancel-stale-child-pipelines.py + job sweep:stale-child-pipelines qui ne tourne QUE sur pipeline planifiee, et — l'inverse, qui est l'erreur a eviter — ancre .not-on-schedule sur les 13 jobs et 3 templates qui n'ont rien a faire dans un balayage, pour qu'une pipeline nocturne ne rejoue pas la CI complete chaque nuit ; make check-nightly-sweep-isolation verifie la separation DANS LES DEUX SENS pour qu'elle ne se defasse pas en silence. Schedule 4397979 (0 3 * * * UTC, ref master) ACTIVE le 2026-08-20 — il etait volontairement inactif tant que !108 n'etait pas fusionnee, un schedule pointant sur un job inexistant n'ayant rien a balayer ; premier tir 2026-08-21 03:00Z. RESTE A OBSERVER, et c'est pourquoi le ticket n'est pas DONE : que le tir du 21 annule les enfants abandonnes ET LUI SEUL, puis que pipelines?source=parent_pipeline ne montre plus qu'UNE enfant active par ref au lieu d'une par poussee |
| ci-cache-tool-builds          | TODO   | NOUVEAU 2026-08-16 (pjb) : « un jour on voudrait cacher ces builds » — clautolisp, alfe, read-autolisp, libredwg sont reconstruits INTÉGRALEMENT à chaque pipeline, sur chaque runner ; un changement d'une ligne de doc relink les images SBCL et recompile LibreDWG partout. Pas théorique : sur le tag release-1.8.42 c'est précisément ce build LibreDWG qui a épuisé la mémoire du poste Windows (cc1.exe out of memory, d'où le plafond -j2). TROIS morceaux séparables par risque croissant. (1) LibreDWG + shim = le gain net, clé = commit du sous-module + identité du compilateur + os/arch + flags ; une mauvaise clé provoque une reconstruction, pas un artefact corrompu. (2) cache fasl Quicklisp/ASDF, clé = implémentation + version + .asd ; un fasl périmé sort en erreur de compilation. (3) les images des outils elles-mêmes = LE morceau dangereux : un pipeline pourrait LIVRER ce qu'il n'a pas construit, et une image périmée n'échoue pas bruyamment, elle produit une release qui revendique une version qu'elle ne contient pas — donc les jobs release ne doivent JAMAIS consommer un cache de build, ou la clé doit épingler l'arbre source complet ; aucun de nos contrôles ne rattraperait ça. NOTE : sur les runners SHELL le cache est local au disque du runner, donc sans réseau — ce qui compte ici puisque l'upload de 13 Mo depuis le poste Windows échoue de façon reproductible |
| doc-build-info-export-quadratic | DONE | NOUVEAU 2026-08-19 (pjb, après deux MR passant chacune ~1 h dans le job documentation) : « cacher l'export Info séparément du split HTML, mais il faut vérifier ; je pense que ce qui est lent, c'est le split du gros .org ». MESURÉ, et la première moitié de l'hypothèse est fausse : le split coûte 5 s pour les deux formats (1,9 s texte + 3,1 s org, 3022 pages), build-paged-html 2 min 38, et build-paged-info 51 min 19 — 94 % du job — en UN SEUL org-texinfo-export-to-texinfo sur les 95 000 lignes. Cacher l'export séparément aiderait donc peu : ça éviterait de refaire une étape déjà quasi gratuite, en laissant les 51 min intactes précisément quand la spec EST modifiée, c'est-à-dire quand le cache saute. POURQUOI 51 MIN : l'export est super-linéaire — 2378 lignes 1 s, 4757 lignes 2 s, 9514 lignes 6 s, 19029 lignes 27 s ; doubler l'entrée quadruple le temps aux grandes tailles. Le coût n'est pas dans le NOMBRE de pages mais dans la TAILLE d'un export. RÉORGANISATION DE PJB TESTÉE : les .org par entrée existent déjà (build-paged-spec les émet en 3,1 s) ; 100 pages exportées individuellement en 2 s, soit ~60 s extrapolé aux 3022 — 51 minutes deviennent une minute pour une reconstruction complète, et l'export d'une seule page (~20 ms) plus un cat pour le cas qui l'intéresse. Donc l'idée est bonne, pour une meilleure raison que celle avancée : N petits exports d'un algorithme quadratique battent un gros par le carré du rapport. RESTE À VÉRIFIER avant de l'implémenter, et non prouvé ici : chaque page s'exporte en manuel AUTONOME (@setfilename, hiérarchie de nœuds propre), donc l'assemblage en UN manuel Info demande de découdre ces en-têtes ou de passer par @include, et les références croisées entre entrées, qui résolvent aujourd'hui parce que tout est dans un seul export, doivent rester valides entre fichiers. Les 60 s sont le coût des exports, pas d'un manuel fini. PREMIER PAS MOINS CHER si la réorganisation attend : ne lancer l'export Info que sur la lane de release et non sur chaque MR — le manuel Info est un produit de release, alors que HTML et PDF sont ce qu'une MR a besoin de voir vérifié ; c'est un changement d'une ligne qui sort les 51 minutes du chemin des MR. Note : le job PARAÎT planté (aucune sortie pendant 51 min), ce qui m'a fait conclure à tort à un blocage — une ligne de progression toutes les N sections rendrait la différence visible. MITIGATION FAITE 2026-08-19 (1.8.55, pjb « fais le changement d'une ligne pour la lane de release ») : SPEC_PAGED_TARGETS (autolisp-spec/Makefile) nomme les dérivés que `paged' construit, défaut `pages html info' ; le job documentation des MR passe `pages html', la lane de release garde le défaut. Rien ne cesse d'être construit — ça cesse d'être RECONSTRUIT sur un chemin où personne ne le lit. Une MR touchant la spec passe d'environ 55 minutes à environ 4. Vérifié par make -n depuis le sous-projet ET depuis la racine, avec et sans la variable : la recette build-paged-info disparaît avec, est présente sans, et les variables de ligne de commande se propagent au sous-make. LE RACCOURCI EST ASSERTÉ, PAS SUPPOSÉ : l'installation est gardée par if [ -f ], donc une release dont la construction Info aurait silencieusement échoué livrerait un artefact de documentation sans le manuel sans rien dire — la forme d'échec exacte que ce dépôt n'arrête pas de rencontrer ; release:documentation teste maintenant la présence du .info et échoue s'il manque. RESTE OUVERT : c'est de l'ordonnancement, pas une correction — l'export reste quadratique et coûte toujours 51 minutes quand il tourne, la lane de release les paie encore, et c'est la réorganisation par entrée qui le rendrait réellement bon marché. RESOLU 2026-08-20 (1.8.61), et PAS par la reorganisation : personne n'avait demande POURQUOI l'export est quadratique. Mesure — le profileur d'echantillonnage d'Emacs ne collecte RIEN sous --batch (TOTAL-SAMPLES=0), donc instrumentation par advice-add des fonctions suspectes, a DEUX tailles, une seule taille ne distinguant pas une grosse constante d'un mauvais exposant : org-texinfo--get-node passe de 2,20 s sur 4,13 s (53 %) a 2 190 headlines a 24,07 s sur 29,34 s (82 %) a 4 931, soit 2,25x les noeuds pour 10,9x le temps. Cause lisible dans ox-texinfo.el : la fonction garde toutes les paires (datum . nom) dans UNE liste d'association et la parcourt deux fois par noeud — (assq datum cache) et (rassoc name cache) — donc O(N^2) en noeuds, et pire quand des noms se collisionnent puisque chaque salage rebalaie. Ce n'est donc pas « le spec est trop gros », c'est une structure lineaire la ou il faut une table de hachage. CORRECTIF : autolisp-spec/scripts/spec-texinfo-fast-node.el remplace cette seule fonction en gardant l'algorithme ET l'ordre de salage ; export du spec complet 51 min -> 60 s, make info (makeinfo strict compris) -> 1 min 14 s. SORTIE IDENTIQUE, verifiee octet pour octet sur deux prefixes reels (2 190 et 4 931 headlines) et sur le corps du fichier complet. RISQUE NOMME : c'est la surcharge d'une fonction INTERNE d'org, epinglee a une forme que la prochaine version peut changer, et une divergence silencieuse donnerait d'autres noms de noeuds — ce qui se manifeste non pas comme une erreur de build mais, bien plus tard, comme un renvoi mort ; d'ou make -C autolisp-spec check-info-export-equivalence (3 s, avant le build dans le job documentation), qui exporte un prefixe du VRAI spec dans les deux modes et exige l'identite octet pour octet — verifie dans les deux sens, il sort 1 en citant la premiere ligne divergente sur une surcharge delibrement faussee. La mitigation de 1.8.55 (SPEC_PAGED_TARGETS="pages html" sur les MR) est RETIREE, ce qui est un gain de couverture et pas seulement de temps : tant que seule la lane de release construisait le manuel Info, une modification du spec qui cassait ce build ne pouvait etre decouverte QUE sur une release. La reorganisation en 3022 exports separes devient sans objet, avec les deux questions ouvertes qui la bloquaient (assembler 3022 manuels autonomes, faire survivre les renvois entre fichiers) ; reste si on veut le rendu HTML incremental (2 min 38), plus petit et sans rapport |
| release-arm32-thread-less-sbcl | TODO | NOUVEAU 2026-08-18 : pjb « fix arm32 so it's not manual on release tags » — FAIT (release:linux:arm32 porte *on-release-arm32, automatique sur tout tag release-*, opt-out SKIP_ARM32_RELEASE=1). Le rendre automatique a révélé qu'il ne PEUT PAS aboutir : toutes ses « réussites » antérieures étaient fictives (un exit 0 dans son before_script terminait le job en vert en 35 s sans rien construire — corrigé 1.8.47). Première vraie tentative, tag release-1.8.47 : 44 minutes puis LOAD-SYSTEM-DEFINITION-ERROR sur bordeaux-threads « This implementation is unsupported » — le SBCL armhf de Debian est SANS THREADS et bordeaux-threads refuse de se charger, erreur au CHARGEMENT, pas dégradation à l'exécution. Le commentaire de la lane anticipait bien un runtime sans threads (« clautolisp tolère, seul le mode session aldb threadé échouerait ») : vrai de clautolisp, faux de son graphe de dépendances — le build n'arrive jamais assez loin. CCL n'est pas une issue (armcl faute sous qemu-user). D'où allow_failure: TRUE sur cette ancre, contrairement à Windows/macOS (false) : ces deux-là sont INTERMITTENTES, celle-ci est BLOQUÉE, et un arm32 obligatoire bloquerait CHAQUE release pour une cause qu'aucun réglage CI ne corrige. Conséquence assumée : 15 artefacts sur 17 tant que ce n'est pas résolu, la collecte journalisant quelles cibles l'ont effectivement atteinte. Quatre options dans le ticket ; la n°3 (rendre la dépendance bordeaux-threads conditionnelle) est la seule sans coût matériel et la seule qui nous apprendrait quelque chose de vrai sur le graphe. OPTION 3 EXAMINEE 2026-08-20, et elle n'est PAS petite : un seul systeme tire bordeaux-threads (clautolisp/autolisp-debug, clautolisp.asd:560), donc la dependance n'est ni accidentelle ni transitive — c'est le moteur du debogueur qui la demande — et il s'en sert pour de bon dans 3 fichiers (breakpoints.lisp : make-lock, make-condition-variable, with-lock-held, condition-notify, condition-wait ; session.lisp : make-thread, current-thread ; snapshot.lisp : current-thread). Ce ne sont pas des appels qu'on saute a l'execution : ils sont ecrits bordeaux-threads:make-lock, donc le paquet doit exister A LA LECTURE. « Rendre la dependance conditionnelle » veut donc dire un paquet de compatibilite (verrous inertes, make-thread en ligne) ou une compilation conditionnelle qui coupe le moteur du debogueur en deux — et derriere, une DECISION DE CONCEPTION : clautolisp pretend-il tourner sans threads ? Le commentaire de la lane l'affirme, mais c'est justement le mode qui apporte la dependance. La phrase du ticket « elle ne coute rien » doit donc se lire « elle ne coute pas de MATERIEL ». PISTE MOINS CHERE, NON VERIFIEE : le refus vient de bordeaux-threads-v0.9.4, et c'est la branche 0.9/API v2 qui a abandonne le support des implementations sans threads, les 0.8.x le portaient — epingler une 0.8.x debloquerait peut-etre la lane SANS toucher au code, au prix d'une contrainte de version Quicklisp ; a mesurer sur une armhf avant d'y croire, personne ici n'ayant de SBCL sans threads sous la main. LANE DESACTIVEE 2026-08-21 en coupant 1.9.0 — pjb : « I think we'll have to disable arm32 release for now », avec l'erreur exacte du ticket (job 16026668734). Deux tags mesures disent la meme chose : la lane tournait sur chaque tag en allow_failure au motif qu'elle « contribue quand elle peut », et elle n'a JAMAIS abouti — 44 min sur release-1.8.47, 48 min (2 908 s) sur release-1.9.0. Une lane qui ne peut pas aboutir ne contribue pas : elle depense trois quarts d'heure de runner emule et tient collect derriere elle, a chaque release. LE SENS DU COMMUTATEUR EST INVERSE, et c'est le point : Windows et macOS sont opt-OUT parce qu'INTERMITTENTES (la machine est la, ou on la declare absente, et un echec y est un vrai echec) ; armv7 n'est pas intermittente, elle est BLOQUEE, donc son defaut est OFF et le commutateur est positif — ARM32_RELEASE=1 la rallume. SKIP_ARM32_RELEASE n'est deliberement PAS garde comme alias : une variable « ne saute pas cette lane » pour une lane qui ne tourne plus par defaut se lirait comme la preuve qu'elle tourne encore. allow_failure reste vrai pour l'execution opt-in, de sorte que rallumer la lane pour essayer un correctif ne puisse pas bloquer une release — c'est d'ailleurs a cela qu'elle servira, ARM32_RELEASE=1 etant la maniere de VERIFIER la piste qui aboutira. AGENTS.md, le commentaire de collect:release et le Context du ticket sont mis a jour ; le ticket reste OUVERT, c'est la lane qui est desactivee, pas la question |
| windows-exe-suffix-callers-not-swept | TODO | NOUVEAU 2026-08-21, trouve en coupant 1.9.0 : les DEUX benchmarks CAO Windows sont morts sur la pipeline du tag (jobs 16026668742/3) avec « /bin/sh: .../bin/alfe-sbcl: cannot execute binary file: Exec format error ». Ni un probleme de CAO ni de benchmark : le fichier n'existe plus sous ce nom. CAUSE — un correctif dont les CONSOMMATEURS n'ont pas ete balayes : alfe-sbcl-windows-build-missing-exe-suffix a corrige le PRODUCTEUR (generate.lisp demande explicitement le suffixe .exe a save-lisp-and-die sous Windows), ce qui est juste et fait marcher build:alfe:windows, mais renommer ce qu'un build produit casse tout appelant qui epelle l'ancien nom, et personne n'est alle les chercher. autolisp-benchmark/Makefile epelait le nom sans extension — sous POSIX c'est le fichier, sous Windows ce n'est rien, et un nom sans extension n'y est pas executable du tout. C'est la forme de defaut qui n'apparait que sur la seule plateforme que personne ne fait tourner en local, et il est apparu sur un TAG plutot que sur une MR parce que les benchmarks Windows sont les derniers jobs. CORRIGE : le Makefile du benchmark recoit le meme mecanisme EXE que celui d'autolisp-front-end (copie, pas reinvente, pour que les deux s'accordent), verifie dans les deux sens depuis un hote POSIX via make -n HOST_UNAME=MINGW64_NT-10.0 -> alfe-sbcl.exe, et sans -> alfe-sbcl. SECOND APPELANT trouve en balayant, et ma premiere lecture etait FAUSSE : j'avais ecrit que les deux ALFE_BIN sans extension de .gitlab/native.yml partaient vers des scripts run-*.ps1 qui font eux-memes la danse du .exe ; un seul le fait — harvest:sysvars:bricscad:windows le passe droit a make save-sysvars, donc rien ne repare le nom en aval, et il aurait echoue pareil. Corrige aussi. LE POINT GENERAL, a garder : un build qui change le NOM de ce qu'il produit doit etre suivi d'un balayage de qui prononce ce nom ; le premier appelant a ete trouve par un job rouge, le second seulement en regardant. NON VERIFIE : que les benchmarks REUSSISSENT ensuite sous Windows — celui de BricsCAD a une reference connue (1 m 37) donc il devrait revenir, celui d'AutoCAD n'a jamais abouti et c'est l'autre moitie de benchmark-autocad-windows-hangs-and-blocks-release |
| libredwg-msys2-compiler-path-lacks-exe | DONE | NOUVEAU 2026-08-23, corrigé en 1.9.8 : sur un répertoire de build FROID sous MSYS2, `make build-libredwg` meurt en configuration cmake — « The CMAKE_C_COMPILER: D:/a/_temp/msys64/mingw64/bin/gcc is not a full path to an existing compiler tool », APRÈS avoir affiché « The C compiler identification is GNU 16.1.0 » deux lignes plus haut : il l'a trouvé, il n'arrive pas à le nommer. CAUSE : la couche POSIX de MSYS2 ouvre `gcc` quand seul `gcc.exe` existe, donc `command -v` répond /mingw64/bin/gcc, `[ -x ]` est vrai et `-dumpmachine` tourne — TOUS les contrôles du script passent ; cmake est un programme NATIF et ne fait pas ce repli. MÊME PIÈGE que windows-release-binaries-lose-exe : un nom que MSYS2 résout et que Windows ne résout pas — à retenir comme une CLASSE, pas comme une coïncidence. Invisible jusqu'ici parce que le runner Windows GitLab nettoie en `-ffd` et non `-ffdx` (volontairement : `-ffdx` meurt quand un process CAO tient un fichier de dist/ ouvert), donc le CMakeCache.txt survit d'une pipeline à l'autre et cmake n'est jamais rappelé à configurer. Révélé par le PREMIER build Windows à froid du projet, sur le runner windows-2025 de GitHub. CORRECTIF : compléter en `.exe` quand le chemin résolu n'a pas d'extension et que `$compiler_path.exe` existe, sous garde MINGW/MSYS/CYGWIN, sans effet ailleurs |
| windows-release-binaries-lose-exe | TODO | NOUVEAU 2026-08-21, P1, signale par pjb : « les executables dans libexec/clautolisp/binaries/windows/x86-64/ n'ont pas l'extension .exe ». CONFIRME sur l'ASSET PUBLIE de release-1.9.0 (zip telecharge et liste) : alfe-sbcl, clautolisp-sbcl, read-autolisp-sbcl, tous sans extension. PIRE QU'UN NOM MAL FORME : les DEUX lanceurs livres dans la meme archive ouvrent <prog>-<lisp>.exe — dispatch.cmd construit le chemin en .exe et refuse avec « no binary for windows/... » + exit 127, dispatch.sh ajoute .exe quand os=windows. Donc LES PRODUITS WINDOWS DE 1.9.0 NE DEMARRENT PAS, avec une archive de 42 Mo parfaitement formee a laquelle il ne manque que trois noms. CAUSE — une courtesie MSYS2 qui transforme le repli en code mort : release-programs copiait avec « if [ -f $b ] then cp $b ... elif [ -f $b.exe ] then cp $b.exe ... », ce qui se lit « le nu, sinon le .exe » et est faux sous Windows d'une maniere invisible a la lecture depuis Linux — la couche POSIX de MSYS2 OUVRE foo quand seul foo.exe existe, donc le test nu est VRAI, la premiere branche gagne toujours, cp vers un REPERTOIRE nomme la destination d'apres le chemin donne (sans extension), et l'elif ecrit precisement pour Windows n'y tourne jamais. La preuve est dans l'artefact : les fichiers copies ont la taille des vrais .exe, donc la SOURCE etait bien le .exe, seule la destination a perdu le suffixe. CORRIGE en deux temps, tous deux porteurs : tester .exe D'ABORD (sous MSYS2 le test nu est ambigu, celui du .exe ne l'est pas) et NOMMER la destination explicitement. ET LE CONTROLE QUI AURAIT DU L'ATTRAPER : verify-release-artefacts ne verifiait que l'existence du fichier et sa taille > 10 000 octets, donc une archive de 42 Mo inutilisable passait — il OUVRE desormais l'artefact binaries et exige les entrees que le lanceur ira vraiment chercher (.exe sous Windows, nu ailleurs), en lisant l'ARCHIVE et non le repertoire de staging puisque c'est l'empaquetage qui a fauté. VERIFIE DANS TROIS DIRECTIONS sur cette machine Linux (REL_OS=windows etant surchargeable, ce qui rend la branche Windows testable sans la machine) : la forme exacte livree par 1.9.0 ECHOUE en nommant ce qu'elle contient a la place, la meme avec .exe passe, et le tar.bz2 POSIX a noms nus passe toujours. CONSEQUENCE A DECIDER : la release publiee n'est pas reparable sur place — make-gitlab-release.py --update ne reecrit que les LIENS, pas ce qu'une pipeline a deja empaquete — donc la reparer demande un nouveau tag, soit release-1.9.1 ; le reste de 1.9.0 est intact (linux-x86-64, linux-arm64, darwin-arm64 nomment correctement, sources et documentation ne portent pas de binaires) |
| benchmark-autocad-windows-hangs-and-blocks-release | TODO | NOUVEAU 2026-08-19 : constaté en coupant release-1.8.49. benchmark:autocad:windows a retenu le runner Windows (concurrence 1) pendant DEUX HEURES, jusqu'au timeout projet, avant que release:windows:x86-64 puisse démarrer ; il avait déjà bloqué release-1.8.47 (annulé à la main). Il n'a JAMAIS réussi, alors que ses voisins sur la même machine tiennent en ~1m30 (bricscad:windows 1m37, clautolisp:windows 1m16) — accoreconsole qui ne rend jamais la main, le mode de panne connu. Deuxième défaut, le plus grave : le blocage est INVISIBLE. Le script fait `make ... *>&1 | Out-File dist/bench/autocad-windows.txt` puis `Get-Content` à l'étape SUIVANTE, donc pendant l'exécution la trace ne montre RIEN, et si le job ne finit jamais le Get-Content n'arrive jamais et la sortie n'est JAMAIS affichée : un job qui pend et un job qui travaille produisent exactement la même trace, impossible de les distinguer de l'extérieur. benchmark est une étape DEVANT release, donc ce job garde les lanes de release sans être lui-même un produit ; allow_failure: true sauve le pipeline mais pas les deux heures. Options 1 (timeout: propre de 5-10 min sur les benchmarks CAD) et 2 (Tee-Object au lieu de Out-File, comme bricscad:macos qui fait déjà `tee`) sont indépendantes de la question AutoCAD et transforment un blocage muet de 2 h en échec lisible de 10 min. FAIT 2026-08-19 (1.8.51, pjb « Fixe le timeout et le Tee-Object sur les benchmarks CAD ») : .benchmark porte timeout: 15 minutes — sur le template de base, pas sur les seuls jobs CAD, car un benchmark est une mesure, jamais un produit de release, et aucun ne doit retenir un runner deux heures devant l'étape release ; les TROIS jobs Windows passent par Tee-Object -Variable puis écrivent la sortie capturée, ce qui rend la trace vivante tout en gardant l'artefact en UTF-8 (Tee-Object -FilePath seul écrit de l'UTF-16LE sous PowerShell 5.1, l'encodage même que le parseur Pages a dû apprendre à encaisser). Étendu à benchmark:clautolisp:windows, qui avait le même motif de trace muette : le laisser aurait conservé un job indiagnosticable. RESTE OUVERT pour la pendaison elle-même : AutoCAD échouera désormais en 15 min avec un log lisible au lieu de bloquer 120 min sans rien — un meilleur échec, pas une correction. Options 3 (sortir benchmark du chemin de release) et 4 (réparer ou désactiver) subsistent |
| release-asset-links-use-artifacts-file | DONE | NOUVEAU 2026-08-19 (pjb, rapporté depuis la CI de SCHMS+) : les liens d'archives des releases renvoyaient la page de NAVIGATION HTML des artefacts (/artifacts/file/) et non le fichier (/artifacts/raw/) ; les deux répondent 200, donc le consommateur écrivait 40 Ko de HTML sous le nom de l'archive et n'échouait qu'à l'extraction, avec un message parlant de format d'archive et non de lien. FAIT 2026-08-19 (1.8.51) : (1) release-1.8.47 ET release-1.8.49 réparées, leurs 15 liens réécrits en raw — vérifié en anonyme sur le lien publié, curl rapporte 246 338 243 octets, file dit bzip2, tar ouvre ; le contournement de run-forest-run/lib-releases.sh n'a plus lieu d'être. (2) scripts/make-gitlab-release.py devient l'étape 6 de la procédure à la place d'un glab release create tapé à la main : il n'émet que du raw et RÉCUPÈRE CHAQUE LIEN avant de créer la release, un text/html avortant la publication — le défaut ne s'annonçait pas, donc le correctif ne pouvait pas être « faire attention ». (3) la liste des assets vient de manifest-release-assets.txt écrit par collect-artefacts depuis le répertoire qu'il vient de remplir, donc plus de liste retapée par release. (4) HORS TICKET mais fatal au correctif : collect:release portait expire_in: 30 days, donc les liens réparés seraient morts vers le 2026-09-17 et toute release pourrissait seule un mois après le dernier regard — passé à expire_in: never, et l'expiration levée sur les deux jobs déjà exécutés. Uploads de projet écartés (all.zip pèse 278 Mo) ; le registre de paquets génériques resterait la maison permanente propre si l'on veut aller plus loin. Garde-fous en échec vérifié dans check-release-lane-integrity.py |
| release-all-zip-appledouble-entries | DONE | NOUVEAU 2026-08-19 : repéré en vérifiant les artefacts de release-1.8.49. clautolisp-1.8.49-all.zip embarque un fichier AppleDouble à côté de chaque répertoire racine (._bin ._etc ._include ._lib ._libexec ._share ._.) ; le .tar.bz2 de la MÊME union est propre. Ce n'est pas cosmétique : les deux conteneurs de la même union ne se dépaquettent donc PAS en le même arbre — la propriété pour laquelle un artefact -all existe — sur Linux/Windows ces fichiers sortent en déchets visibles à côté de chaque répertoire, et manifest-*.txt ne les liste pas, donc l'archive contient des fichiers dont le manifeste de provenance ne rend pas compte. Source probable : les xattrs macOS de la lane darwin-arm64. Option 3 (COPYFILE_DISABLE=1 côté darwin) prévient au lieu de nettoyer, option 1 (zip -X) protège de toute entrée macOS future ; les deux sont bon marché. La vérification a sa place avec l'assertion de disposition déjà posée dans release-sources : après écriture du zip, échouer si une entrée matche ._* — le patron établi par 1.8.47 et 1.8.49. FAIT 2026-08-19 (1.8.51) : corrigé aux DEUX bouts. À la source, pack-release-artefact emballe avec COPYFILE_DISABLE=1, --exclude sur ._* et --no-xattrs, ce qui supprime aussi les en-têtes pax LIBARCHIVE.xattr.com.apple.provenance — le même défaut sous sa forme discrète, qui faisait imprimer 23 fois « Ignoring unknown extended header keyword » à GNU tar sur chaque extraction de l'archive darwin ailleurs ; --no-xattrs est SONDÉ et non supposé, GNU tar et bsdtar n'orthographiant pas leurs options pareil et un tar qui l'aurait rejeté aurait emporté la lane darwin. À l'emballage, le -all.zip et les .zip Windows par cible portent enfin l'exclusion que les branches tar avaient depuis toujours — cette asymétrie ÉTAIT le bug — et la recette RELIT le zip qu'elle vient d'écrire. Vérifié dans les deux sens : le contrôle fabrique désormais ses entrées darwin AVEC des membres AppleDouble (une vérification nourrie d'entrées propres ne distingue pas une exclusion qui marche d'une exclusion supprimée), retirer l'exclusion fait avorter collect-artefacts, retirer aussi l'assertion fait échouer le contrôle de script. Artefacts de 1.8.47 et 1.8.49 non reconstruits : publiés, immuables, et le rebut est inerte à l'extraction |
| case-insensitive-pathname-resolution | DONE | NOUVEAU 2026-08-19 (pjb, rapporté depuis SCHMS+) : un corpus AutoLISP écrit sous Windows/macOS n'a JAMAIS eu la casse de ses chemins vérifiée — ces systèmes replient, donc rien ne la sanctionne — et échoue sous Linux ; dans l'incident d'origine le chemin n'était même pas un littéral, il dérivait d'un NOM DE CLASSE dont la casse est une convention sans rapport avec le disque, donc aucune relecture ne l'aurait vu. FAIT 2026-08-19 (1.8.52) : sysvar CLAUTOLISPCASEINSENSITIVEPATHS (entier, défaut 0, relu à chaque appel donc setvar à chaud), amorçable par la variable d'environnement de MÊME NOM sans soulignés (pjb : le dépôt a deux familles — réglages du programme soulignés CLAUTOLISP_ROOT, amorces de sysvar homonymes SECURELOAD/CLAUTOLISPSUPPORTFILESEARCHPATH — celle-ci amorce un sysvar, donc aucun mécanisme nouveau). Marche composant par composant : exacte d'abord SANS lister le répertoire (la condition de coût du ticket), puis unique à la casse près, puis erreur NOMMANT les candidats ; cache par répertoire qui se répare seul sur un miss, pour ne pas cacher un fichier créé dans la même exécution. findfile rend le nom RÉEL et le test vérifie qu'il se re-résout — c'est la propriété « stocké puis réutilisé ». LECTURE/CRÉATION tranché en TROIS politiques et non deux : :must-exist, :as-written (création — feuille telle quelle, répertoires quand même repliés, donc jamais d'écrasement d'un RAPPORT.TXT préexistant ; s'applique aussi à vl-mkdir, à la destination de vl-file-copy et au nouveau nom de vl-file-rename), et :prefer-existing pour le mode "a" que le ticket demandait de décider explicitement — « ajouter » veut dire à ce fichier s'il existe, sinon le créer, donc il se lit comme une lecture puis comme une création. Avertissement [path-case] nommant l'orthographe réelle, sinon le sysvar ne ferait que glisser le problème sous le tapis. HORS PORTÉE et signalé : (command "_.open" …) et getfiled, qui passent par la couche commande de l'hôte CAO et par un sélecteur de fichiers, donc par aucun des résolveurs branchés. 12 tests verts ; le harnais DEMANDE au système de fichiers s'il distingue la casse plutôt que de nommer des OS (macOS peut être formaté sensible, un Linux peut monter l'inverse), et assère la prémisse quand il saute |
| case-insensitive-paths-ignored-in-cad-dialects | DONE | NOUVEAU 2026-08-19 (pjb, incident SCHMS+) : suite de case-insensitive-pathname-resolution close en 1.8.55 — la fonctionnalite marchait, mais pas la ou elle sert. Le sysvar n'etait enregistre que sous le dialecte clautolisp, donc sous --dialect bricscad-v26 il n'existait pas du tout : ni setvar ni l'environnement n'avaient prise, et CLAUTOLISPCASEINSENSITIVEPATHS=1 ne changeait rien. Or c'est l'inverse de ce qu'il faudrait — on execute un corpus sous --bricscad / --autocad justement pour l'exercer tel qu'il tournera, et un corpus ecrit pour Windows est precisement celui dont la casse des chemins n'a jamais ete verifiee par personne ; le seul dialecte a beneficier de la fonctionnalite etait celui qui en avait le moins besoin. FAIT 2026-08-19 (1.8.56) selon TA DECISION, et non selon l'une des deux variantes du ticket : le sysvar existe sous TOUS les dialectes SAUF strict, en lecture-ecriture, et son defaut reste 0 PARTOUT. Le defaut merite d'etre relu car il contredit l'intuition du ticket lui-meme (« plier la casse y est la fidelite ») : ta raison est que BricsCAD sous Linux n'est pas repute plier la casse sur un systeme de fichiers sensible a la casse, donc plier par defaut serait une invention de clautolisp plutot qu'une fidelite au moteur emule — l'utilisateur demande, ou rien ne se produit. strict reste l'exception pour sa propre raison : signaler ce qu'un moteur portable refuserait. L'amorcage par l'environnement suit la meme regle, une variable ne pouvant semer que dans un sysvar qui EXISTE. Le comportement suit sans autre changement, le resolveur lisant le sysvar sur l'hote a chaque appel. SIX TESTS dont un qui exerce le COMPORTEMENT et pas seulement la presence de la cellule (session bricscad-v26, sysvar a 1, retrouve dial/sigfic.lsp sous DIAL/SIGFIC.LSP ET emet [path-case] — la condition que le ticket posait : la tolerance ne devient pas un silence) ; verifie dans les DEUX SENS, l'ancien gardien when clautolisp-p faisant echouer quatre d'entre eux. Suite complete verte (1948 verifications). Spec corrigee (elle affirmait « Honoured only under the :clautolisp dialect ») et manuel utilisateur augmente d'une section « It works under the CAD dialects too » |
| bricscad-sysvars-on-macos | DRAFT | NOUVEAU 2026-08-19 (pjb, observe sur thalassa) : des sysvars de clautolisp rendent des chemins WINDOWS sous macOS (ACTIVITYINSIGHTSPATH, BMTOOLPATH). Demande en trois points : recenser les sysvars porteurs de chemins avec categorie et sens, decider pour chacun chemin-CAO ou chemin-clautolisp, et produire une table. ANALYSE FAITE 2026-08-19 (1.8.56), en interrogeant le catalogue plutot qu'a la main : sur 1836 sysvars, 57 portent un chemin, et ils ne sont PAS dans le meme etat — CLASSE A, 6 ACTIVEMENT FAUX (chemin Windows litteral ou disposition Windows deguisee : ACTIVITYINSIGHTSPATH AutoCAD, BMTOOLPATH / POINTCLOUDCACHEFOLDER / CLOUDDOWNLOADPATH / CLOUDTEMPFOLDER / IMAGECACHEFOLDER BricsCAD — pjb en avait vu deux, il y en a six) ; CLASSE B, 43 derives de l'hote et laisses VIDES, qui ne mentent pas mais se taisent, la question etant s'il faut les peupler ; CLASSE C, 8 litteraux qui ne sont pas des chemins absolus (acad.lin, « PDF Images », « . », une URL, un nom de profil) et qu'il ne faut pas confondre avec A sur la foi de leur nom. PRECEDENT TROUVE dans l'arbre : TEMPPREFIX est deja peuple au lancement par autolisp-cli/source/transmit.lisp, exactement le traitement demande — il donne le patron. Sur le fond, reproduire le chemin de la CAO serait un contresens : ces repertoires n'existent pas sous clautolisp qui ne peut ni les creer ni les peupler, et un chemin vers un repertoire absent n'est pas plus utile qu'une chaine vide, seulement plus trompeur — donc chemin clautolisp (XDG), documente avec la valeur d'origine. Table de la classe A avec proposition par sysvar dans le ticket, classe B ventilee en quatre categories (ressources d'installation -> rester vides tant que clautolisp ne fournit pas la ressource ; espace utilisateur -> peupler ceux qu'un programme LIT pour decider ou ecrire ; etat du dessin courant -> rien a decider, ils dependent du dessin ; TEMPPREFIX -> deja fait). NON ETABLI et dit plutot que devine : les valeurs REELLES d'AutoCAD et de BricsCAD pour la classe B — le catalogue les donne host-derived donc vides, ce qui signifie que personne ne les a mesurees, pas qu'elles sont vides sur un vrai CAO ; remplir cette colonne demande une sonde sur les deux moteurs, du type des jobs vendor:probes:*, et c'est mecanisable. MESURE FAITE 2026-08-20 sur BricsCAD V26 / macOS (job harvest:sysvars:bricscad:macos, via alfe, 1836 sysvars releves) : sur les 57 porteurs de chemins, 37 ONT UNE VALEUR, 13 sont ABSENTS et 7 sont vides. CELA RENVERSE DEUX DE MES CONCLUSIONS : (a) « 43 host-derived et vides, les laisser vides » est faux, le "" du catalogue voulait dire non mesure et j'en avais quand meme tire une recommandation ; (b) « leur donner des chemins XDG remplacerait une fiction Windows par une fiction POSIX » est faux aussi, car BricsCAD sur macOS LOCALISE tout (~/Library/Application Support/Bricsys pour la config, /Applications/BricsCAD V26.app pour les ressources, $TMPDIR/BricsCAD pour les temporaires, ~/Documents pour l'utilisateur) — donner a clautolisp les equivalents XDG est donc la MEME demarche, l'intuition de pjb etait la bonne. Trois observations qui portent des decisions : BMTOOLPATH vaut EXACTEMENT ce que pjb avait devine dans le ticket ; LOCALROOTPREFIX et ROAMABLEROOTPREFIX sont IDENTIQUES sur macOS (la distinction local/itinerant est une notion Windows non transposee, donc clautolisp n'a pas a inventer deux emplacements) ; LOGFILEPATH pointe sur le workdir d'alfe parce qu'alfe l'a POSE, ce que la lecture du code avait annonce — mesure et code se confirment. RESTE : les trois moteurs Windows (BricsCAD, AutoCAD GUI, AcCoreConsole) sont en file sur le runner CAO, c'est la comparaison GUI/headless demandee. Ticket passe en DRAFT : moitie des mesures faite, aucune decision prise. RECOMMANDATION : traiter la classe A seule d'abord (6 sysvars, correction visible, aucune decision de conception en suspens — un chemin Windows sur macOS est faux quoi qu'on decide ensuite) |
| autolisp-globals-writability-triage | TODO | NOUVEAU 2026-08-20 (pjb, suite de sysvar-table-ignores-runtime-dialect-change) : d'autres variables *AUTOLISP-...* changent-elles le comportement dynamiquement, et faut-il les passer en RO avec une fonction de reglage qui ferait les initialisations, plutot que d'instrumenter toutes les affectations ? TRI FAIT sur les 31 variables, en cherchant qui les LIT apres le lancement (une variable que rien ne relit ne peut rien declencher, quoi qu'en dise le manuel), et en excluant transmit.lisp qui les DECLARE sans les lire. Piege evite : beaucoup de « lecteurs » apparents sont dans alfe, et c'est l'inverse d'une lecture — alfe les INJECTE dans le moteur CAO distant. RESULTAT en quatre classes : (A) affectation qui doit declencher une initialisation = *AUTOLISP-DIALECT* SEULE ; (B) deja correcte car relue a chaque usage = *AUTOLISP-FILE-ENCODING* (via LOOKUP-AUTOLISP-FILE-ENCODING), qui montre le bon patron ; (C) declarative par conception = *AUTOLISP-TERMINAL-ENCODING*, dont le propre texte d'aide dit « No stream rebinding is performed at the CL level » ; (D) faits de lancement que rien ne relit dans clautolisp = les 28 autres. CONSEQUENCE : la troisieme voie envisagee par pjb (surveiller les affectations) est disqualifiee par le tri — elle couterait sur chaque setq du langage pour surveiller UNE variable. RESERVE SERIEUSE sur la voie RO : (setq *AUTOLISP-DIALECT* 'lax) est une interface DOCUMENTEE writable (point explicite d'alfe-clautolisp-dialect), donc la passer en RO casse du code utilisateur — a assumer ou a deprecier, pas a decouvrir. RECOMMANDATION : voie 2 du ticket dependant — rendre la LECTURE de la table des sysvars consciente du dialecte (vue calculee plutot que table estampillee au lancement) ; aucune fonction de reglage necessaire, l'interface documentee est preservee, et les deux sens du defaut disparaissent. La voie RO+setter redevient interessante le jour ou une SECONDE variable rejoint la classe A |
| sysvar-table-ignores-runtime-dialect-change | DONE | **RÉSOLU 1.9.9.** (setq *AUTOLISP-DIALECT* …) déplaçait tout ce qui dépend du dialecte SAUF la table des sysvars, qui appartient à l'hôte et n'était peuplée qu'une fois, au lancement — donc l'existence des sysvars clautolisp, le masquage de ceux qu'ignore BricsCAD, la lecture-seule de SECURELOAD/TRUSTEDPATHS et l'amorçage par l'environnement étaient figés. CORRECTIF : une vue base + overlay rejouée PARESSEUSEMENT. La base est un instantané pris AVANT tout overlay — c'est elle qui rend l'overlay réversible, puisqu'un overlay RETIRE des cellules et que rien ne peut les dé-retirer sinon repartir d'une table qui les avait. Le déclenchement répond au « qui remarque le changement ? » du ticket : PERSONNE — chaque accès sysvar compare d'un EQ le dialecte courant à celui que la table reflète. Les 4 questions tranchées comme le ticket l'indiquait ; masquage AVEC MÉMOIRE, les valeurs posées étant réappliquées À TRAVERS SETVAR et non forcées (sous un dialecte qui cache ou verrouille le sysvar elles restent mémorisées et non appliquées — forcer inventerait un état de moteur). Vérifié sur la trace du constat : strict:nil bricscad:1 back-strict:nil bricscad-again:0. TROIS BOGUES TROUVÉS EN CODANT, dont deux SILENCIEUX : (1) une methode ajoutee a un protocole sans importer le symbole ne rate pas, elle reussit AILLEURS — cador a defini des generiques homonymes dans son propre paquet, zero avertissement ; (2) copy-sysvar-cell n'etait exporte par aucun paquet ; (3) un fichier de tests sans (in-suite …) compile, se charge et NE TOURNE PAS — mes 4 tests ont d'abord rendu la suite VERTE en ne s'executant pas, le pire des trois puisqu'il se presente comme un succes |
| aldo-config-path-implemented-twice | DONE | **RÉSOLU 2.0.1.** Trouvé avec l'outil de pjb (check-duplicate-symbols). =ALDO-CONFIG-{RELATIVE,SAVE,LOAD}-PATH= et la logique XDG dessous existaient DEUX fois, en symboles distincts dans =CLAUTOLISP.DEBUG.UI= (exportés) et =AUTOLISP-BUILTINS-CORE= (internes). Elles s'accordaient, donc rien ne se voyait : le défaut était latent — le jour où l'une apprend une variable d'override de plus, le débogueur SAUVE là où le builtin ne LIT plus. CAUSE STRUCTURELLE, pas de l'étourderie : builtins-core est SOUS debug-ui dans la pile et ne peut pas en dépendre, donc le builtin a réécrit ce qu'il ne pouvait pas appeler. CORRECTIF comme pjb l'a demandé : un module =clautolisp/configuration= au BAS de la pile (uiop seul), que les deux voient. L'implémentation du debug-UI y est DÉPLACÉE SANS MODIFICATION — elle était la canonique (paramétrée par nom, servant déjà aldo.conf et lisp.conf), et déplacer plutôt que fusionner garantit que le commit ne change OÙ personne ne trouve sa configuration. Le module DÉCLARE la structure : =*configuration-names*= = ("aldo" "lisp"). debug-ui IMPORTE puis ré-exporte les cinq noms (mêmes symboles) ; builtins-core perd 1 222 caractères de copie. LE TEST QUI COMPTE épingle l'IDENTITÉ DES SYMBOLES, pas l'égalité des résultats : les deux copies calculaient des chemins corrects, identiquement, pendant des mois — un test sur les résultats serait resté VERT toute la durée de la duplication. NON FAIT, pjb le dit non urgent : le rassemblement avec les options ; seul =config-getenv= est remonté, avec le cas « variable exportée mais VIDE » sans lequel toute configuration se résoudrait depuis la racine |
| transpiler-t-is-not-self-evaluating | DONE | **RÉSOLU 2.0.2**, trouvé en écrivant la tranche des appels du compilateur. Le transpileur compilait TOUTE référence de symbole en =LOOKUP-VARIABLE=, =T= compris ; l'interpréteur a un cas explicite avant : =T= s'évalue en lui-même QUELLE QUE SOIT sa liaison. Reproduit : =(progn (setq t nil) (cond (t 42)))= rend 42 interprété, NIL compilé. PORTÉE au-delà du =(setq T …)= : sans ce cas, =(cond (T …))= tombe silencieusement dans le vide pour tout dialecte en mode =:silent-nil=, c'est-à-dire tous les profils produit. POURQUOI LES TESTS NE L'ONT PAS VU — deux camouflages superposés : dans le contexte par défaut =T= EST lié à lui-même, donc lookup rendait juste ; et le corpus contenait bien deux =(cond … (t …))= mais avec une clause antérieure vraie, la clause =t= n'était JAMAIS PRISE. Leçon portée au corpus : un cas doit PRENDRE la branche, pas seulement la contenir. DEUX DURCISSEMENTS de la même famille faits en même temps — le compilateur qui garde un second avis sur une question dont le runtime a la réponse : =%SELF-EVALUATING-P= (copie locale, SUR-ENSEMBLE : elle déclarait auto-évaluant tout objet non-cons non-symbole là où l'interpréteur signale =:invalid-form=) remplacée par =SELF-EVALUATING-RUNTIME-VALUE-P= désormais exporté ; et =%OPERATOR-NAME= mis en majuscules comme =SPECIAL-OPERATOR-NAME=, inoffensif tant que tout retombait sur l'interpréteur, dangereux dès que les appels se compilent |
| cloud-platform-runners        | TODO   | pjb: survey on-demand macOS/Windows cloud runners. RELEVÉ 2026-08-21 (pjb : « pour builder une release on a besoin d'une machine windows, mais PAS de cad dessus ; docker ou qemu ou virtualbox ? »). La scission est l'idée utile : les lanes CAO exigent le PC physique et ses applis licenciées, la lane de BUILD ne demande que MSYS2+SBCL+CCL+cmake (tout scripts/release-windows.sh), et seule la seconde est sur le chemin de release. (a) Conteneurs Windows sur hôte Linux : IMPOSSIBLE, il leur faut un noyau Windows — ce qu'on vend comme « Windows in Docker » est une VM QEMU emballée. (b) VM KVM sur poseidon : disponible TOUT DE SUITE, vérifié — i9-9900K 16 threads avec vmx, /dev/kvm présent, kvm_intel chargé, 125 Go de RAM, 717 Go libres, qemu/virsh/virt-install déjà installés ; le coût n'est pas la machine mais la LICENCE (éval Server 2025 = 180 j, activation obligatoire sous 10 j, `slmgr /rearm` jusqu'à 6 fois) plus une journée de provisionnement à maintenir. (c) Compte gratuit Azure : les 750 h/mois sont réelles mais les trois tailles offertes (B1s, B2pts v2, B2ats v2) ont 1 Gio de RAM et l'offre dure 12 mois — presque sûrement trop petit pour dumper des images SBCL, mais c'est un RAISONNEMENT, pas une mesure : 20 minutes suffiraient à trancher. (d) windows-latest hébergé par GitHub : GRATUIT ET ILLIMITÉ sur dépôt public, 4 vCPU/16 Gio (2/8 en privé), ogamita/clautolisp EST public, et .github/workflows/release.yml porte DÉJÀ l'entrée de matrice windows/x86-64 → windows-latest dont l'étape de build est un stub « TODO Windows packaging ». Recommandation : remplir ce stub d'abord. L'objection de LIVRAISON est plus faible qu'elle n'en a l'air (vérifié sur l'API Releases, après la question de pjb « peut-on ajouter des artefacts APRÈS la création ? ») : un asset de release GitLab est TOUJOURS un lien — GitLab ne stocke jamais de fichier — et les liens s'ajoutent (POST), se repointent (PUT) et se suppriment (DELETE) à tout moment, noms et URL devant être uniques dans la release. Une release peut donc être publiée avec ce qui existe puis GAGNER ses produits Windows ensuite ; rien n'a besoin d'être téléchargé dans un job GitLab, l'archive n'a besoin que d'une URL stable. À adopter au passage : `direct_asset_path`, qui donne au lien une adresse permanente servie par GitLab (/releases/<tag>/downloads/<chemin>) qu'on repointe par-dessous — nos liens sont épinglés à un ID DE JOB aujourd'hui, donc re-lancer collect:release les périme. DÉCIDÉ le jour même : pjb a chiffré Azure à 140 $/mois pour une configuration utilisable — « Let's just do github for now ». La lane windows-latest est ÉCRITE (.github/workflows/release.yml : MSYS2 en MINGW64, SBCL par Chocolatey, puis scripts/release-windows.sh — le MÊME script que la lane GitLab, une seule recette Windows) et NON PROUVÉE : écrite d'après le contenu publié de l'image windows-2025, pas d'après une exécution verte. BLOQUÉ POUR ITÉRER : sans jeton GitHub, l'API publique donne l'état par étape mais actions/runs/:id/logs répond 403 — on voit QUELLE étape échoue, jamais POURQUOI ; et workflow_dispatch ne peut pas être déclenché non plus. Il faut un jeton (Actions read + workflow) ou un `gh auth login`. Reste aussi : le job collect ne réunit toujours pas les artefacts par cible, et il faut choisir comment l'archive Windows rejoint la release GitLab. Mesuré au passage : la CI GitHub était VERTE sur release-1.9.2 à 14h34 pendant que tous les jobs GitLab échouaient à cloner. KVM-sur-poseidon reste le repli. Dans tous les cas le PC Windows physique reste : c'est l'hôte CAO |
| cecil-native-arm64-runner     | TODO   | NOUVEAU 2026-08-21 (pjb) : deux runners sur *cecil*, un DGX Spark Ubuntu arm64 — un shell, un docker, tous deux acceptant aussi les jobs sans tag. C'est la première machine arm64 tournant Linux NATIVEMENT (thalassa offre déjà de l'arm64 natif via Docker, mais dans une VM sur macOS) ; l'une comme l'autre valent mieux que ce qu'utilise release:linux:arm64 aujourd'hui, l'émulation qemu-user sur poseidon. C'est une machine de bureau mais ça n'achète aucune disponibilité pour l'instant — pjb : « pour le moment elle se comporte comme un laptop, avec horaires de bureaux » — donc aussi intermittente que thalassa, et le DÉFAUT de la lane arm64 doit RESTER sur poseidon même quand cecil aura fait ses preuves : un tag se pousse quand la release est prête, pas quand une machine est réveillée. Émulé-mais-toujours-là bat natif-mais-parfois sur le chemin de release ; le gain de cecil est ailleurs. pjb en donne la mesure : pour l'arm64 c'est une SECONDE machine plus grosse, pas une machine unique — probablement plus rapide que thalassa et surtout beaucoup plus de mémoire, donc plus de jobs EN PARALLÈLE (capacité, pas architecture — raison de plus pour ne pas la dépenser sur une lane de release, qui est un seul job). Sa seule capacité unique est le GPU : tag `nvidia`, usage attendu = jobs LLM/ollama, qu'on aura à tester et déboguer et que poseidon ne fait qu'en CPU. Aucun tel job n'existe encore ici ; le tag existe pour que le premier ait de quoi le demander. Ils sont arrivés tagués `aarch64`, hors de la convention `<os>,<arch>,<executor>`, et le shell sans tag d'exécuteur — donc aucun job ne matchait. ÉTAPE 1 FAITE le jour même : pjb a retagué en `linux,arm64,{shell,docker}` (+`nvidia` en tag de capacité), de sorte que le dépôt n'a pas à apprendre une seconde orthographe de la même architecture. RESTE l'étape 2 : aucun job ne DEMANDE `linux,arm64,docker` — la lane arm64 demande `linux,arm64,qemu`. Le basculement est d'une ligne (GitLab interpole les variables dans `tags:`) mais il est sur le CHEMIN DE RELEASE, inexécutable tant que le quota bloque la CI, et un tag mal interpolé retirerait arm64 d'une release en silence — la forme exacte des trois fautes de 1.9.0. Il entrera quand une pipeline pourra le vérifier. Ne résout PAS arm32 : les cœurs Grace sont AArch64 seulement |
| release-1.9.0                 | TODO   | NOUVEAU 2026-08-15 (pjb) : « avant de commencer cadtui, il faudra bumper la version a 1.9.0 et faire une release ». PORTE, PAS ACTION IMMÉDIATE — pjb le même jour : « ne bump pas la version tout de suite, on a peut-être d'autres issues à implémenter avant ». Donc on continue à bumper DEVELOP normalement (1.8.37, 1.8.38…) et tout ce qui se termine d'ici là part DANS 1.9.0 ; les stamps sont posés une seule fois, dans le commit de release. `m' et non `d' parce que la série 1.8 a apporté des changements visibles (codecs single-octet indépendants de l'hôte, lisp.conf, format [tag] + annexe, fichiers de conf auto-documentés). MESURÉ contre release-1.8.0 : clautolisp 1.8.36 → 1.9.0 et alfe 1.8.3 → 1.9.0 (11 commits), mais read-autolisp reste à 1.6.0 — 0 commit, et la règle autorise un programme inchangé à garder un A.B plus ancien ; ça a l'air faux et c'est correct. check-versions passe aujourd'hui (14 releases, 10 séries). Attention au tag : sur un release-* tout le pipeline tourne et les jobs release-mandatory le BLOQUENT, donc les runners natifs doivent être en ligne — dont le portable CAD, ~30 min de balayage |
| nice-make-output              | DEFERRED | concise per-target make output — REPORTÉ 2026-08-11 sur sa propre condition : le ticket dit « we'll do that at the end, when everything is mostly debugged and we don't need the whole commands ». Cette condition n'est pas remplie (épiques compiler / drawing-database-api ouvertes) et la session 1.8.28 a montré deux fois le contraire : le dump brut est ce qui a permis de diagnostiquer le MISSING-DEPENDENCY babel et l'échec de clé de commande. À reprendre quand les lanes CI natives seront stables ; ~1040 lignes de recette sur 7 Makefile, sans include commun, donc prévoir un common.mk + DEBUG=1 forcé en CI |
| lisp-configuration            | DONE   | RÉSOLU 1.8.29 : les 4 items du « To do » + le routage. Le blocage « on hold » est levé en prenant (b) — la recommandation du ticket lui-même : les couches ne contiennent QUE les réglages explicites, les défauts deviennent une 3e couche résolue au lookup. C'est ce qui rend l'empilement possible : tant que la couche aldo était amorcée depuis les défauts, toute clé commune paraissait présente et lisp.conf n'était jamais atteignable depuis DBG>. Livré : *LISP-CONFIGURATION* + lisp.conf (mêmes règles XDG), CONFIG-EXPLICIT, LISP-SETTING / DEBUGGER-SETTING, SET-LISP-SETTING, commandes REPL ,set (nu = liste en marquant les défauts) et ,write-settings, bridge SEDIT du débogueur passé sur DEBUGGER-SETTING, et CLAL-SEDIT routé via un nouveau hook clautolisp.sedit:*default-on-quit-policy* (hook et non appel direct : la config vit dans DEBUG.UI, au-dessus du système sedit). Écart assumé : la paire ,set / ,settings demandée est IRREPRÉSENTABLE — le framework impose que la clé soit l'initiale des mots, et set/settings donnent tous deux « s ». Vérifié sur le binaire + 8 tests neufs (SBCL 6369 / CCL 6372, 0 échec). UNE conséquence visible à valider par pjb : un fichier de conf ne contient plus qu'un diff par rapport aux défauts (lecture d'un ancien fichier complet inchangée). SUITE 1.8.30 sur demande pjb (« write the settings that have their default value in a comment, so the user may know what is available ») : le fichier sauvegardé se DOCUMENTE lui-même — sous les réglages modifiés, chaque réglage laissé au défaut est écrit commenté, à sa valeur par défaut et suivi des valeurs acceptées. Les commentaires sont de la doc, pas des données : le reader les saute, donc charger un tel fichier ne rend explicite que les entrées vives et l'empilement aldo-sur-lisp reste intact (test dédié). C'est (b) qui rend la distinction possible. Au passage DEUX défauts corrigés, révélés par les tests neufs : les symboles sortaient entourés de barres verticales (navigator échappé en barre-navigator-barre) — illisible pour un fichier qu'on édite à la main, et pré-existant ; et lisp.conf documentait les réglages d'ALDO (save-lisp-configuration appelait write-aldo-configuration). issues/closed/ |
| windows-terminal-encoding-fd-stream | TODO | -Eterminal must use Console API (SetConsoleOutputCP) on Windows, not fd reopening; stopgap warns-and-keeps-default (1.7.21) |
| cp1252-codec-host-independent | DONE   | RÉSOLU 1.8.28 : codec single-octet intégré (flux Gray), tables construites DEPUIS BABEL — pas écrites à la main, babel était déjà une dépendance du dépôt et livre les mêmes tables sur SBCL et CCL. Le trou n'était pas CP-1252 mais CP-125x+KOI8 : CCL n'a AUCUN des cinq. Chemin natif inchangé là où l'hôte fournit le codec (rien ne bouge sur SBCL). 3 sites, pas 1 : OPEN, le validateur CLI `-e' (`-e cp1251' était une usage-error sur CCL) et le diagnostic ENC-UNSUPPORTED-TARGET (il aurait averti contre une opération qui marche) ; LOAD avait le même trou. Preuve octet : € -> 0x80 et é -> 0xE9 sur les DEUX hôtes (€ est le caractère discriminant, é seul ne prouve rien). Stopgap de skip supprimé : 8/8 lignes sur CCL, 0 skip dans tout l'arbre. Bug préexistant corrigé au passage : `--list-encodings' appelait CCL::MAP-CHARACTER-ENCODINGS, inexistant dans toute version de CCL. issues/closed/ |
| alfe-bricscad-argv-template-ccl | TODO | NOUVEAU 1.8.28 : BUILD-LAUNCH-ARGV perd le template "C:/t.dwt" sous CCL (pathname drive-letter ?) ; reproduit à l'identique sur origin/master, donc préexistant. Surtout : `make -C autolisp-front-end test-ccl' sort 0 malgré l'échec — la lane alfe CCL ne peut pas échouer |

** Debugger
| Issue                                | Status   | Note |
|--------------------------------------+----------+------|
| debugger-public-interface-and-on-error | TODO   | clal-* API + --on-error/--debugger-ui/--aldb-* CLI |
| deferred-debugger-compiler-two-bodies | DEFERRED | gated on compiler (Phase 8) |
| deferred-debugger-remote-cad          | DEFERRED | gated on transport + CAD (Phase 9) |

** DCL (dialog control language)
| Issue               | Status | Note |
|---------------------+--------+------|
| dcl-tui-completion  | IN PROGRESS | TUI complete (1.7.18): clusters expand, all interactive tiles round-trip, --dcl tui\vert{}gui\vert{}auto selection, $reason breadth, all-widgets + listpick examples/tests green; remaining = the Qt C++ GUI app (item 6, opendcl epic) |

*** OpenDCL (feature build; no code yet — one epic per opendcl.plan)
opendcl-runtime-core, opendcl-controls, opendcl-host-pickers,
opendcl-project-file, opendcl-renderer-scaffold, opendcl-wire-protocol,
opendcl-conformance. All =TODO=.

** Compiler
| Issue    | Status | Note |
|----------+--------+------|
| compiler | TODO   | flagship epic (DRAFT): interpreter companion + AutoLISP→CL compiler |

** Spec / docs
| Issue                              | Status  | Note |
|------------------------------------+---------+------|
| autolisp-spec-load-open-encoding-docs | DONE | document load/open file-encoding behaviour (BOM, UTF-8/16). FAIT 2026-08-19 (1.8.53) : entrée `open` — section File encoding avec les trois valeurs documentées (utf8 / utf8-bom / utf16-bom), qualifiées par version (AutoCAD 2021, BricsCAD V21, absentes avant), le repli SYSCODEPAGE, texte vs binaire, plus la règle de cohérence que le ticket demandait d'asserter — (= (chr N) <même caractère décodé d'un fichier>), là où le chr natif de BricsCAD diverge et que alfe honore en transcodant en UTF-8+BOM. Entrée `load` — l'ASYMÉTRIE d'abord, car c'est elle qui surprend : open a gagné un argument d'encodage en 2021, load NON, donc l'encodage d'une source est décidé par ses premiers octets seuls ; support Unicode V20, petit-boutien, « le BOM est absolument requis », et la conséquence écrite en toutes lettres — une source cp1252 sans BOM n'est pas chargeable portablement, et sous macOS (SYSCODEPAGE=MAC_ROMAN) elle se décode faux SANS qu'aucune erreur ne soit levée : le fichier se lit, le programme tourne, les chaînes sont fausses. Section Pre/Post-Unicode étoffée d'un tableau contrastant les deux points d'entrée et de la phrase de Bricsys qui explique toute l'histoire macOS. Manuel utilisateur : nouvelle sous-section « Writing sources that load on every engine » — écrire de l'UTF-8 AVEC BOM, et pourquoi, la règle étant celle des moteurs CAO et non de clautolisp, et échouant en silence. NON FAIT et signalé : les deux alignements secondaires (file-compat usage.org, specs runtime), qui décrivent le comportement de clautolisp, non mis en cause ici |
| autolisp-spec-questions            | BLOCKED | COND edges etc.; needs cad-access |
| tex-pdf-characters                 | DONE    | extra characters needed for the PDF build. FAIT 2026-08-19 (1.8.54) : deux points de code manquaient, tous deux dans clautolisp-sedit-spec.org — U+2698 ⚘ (dans DejaVu SANS et Symbola, absent de DejaVu SERIF qui est la police du corps) et U+27FA ⟺ (dans DejaVu SERIF, absent de DejaVu Sans MONO). Le second est le plus instructif : org rend =code= et verbatim par la fonte MONO, donc cette flèche s'affichait en prose et disparaissait dans un span de code, dans le MÊME document et la même famille de polices — une vérification qui ne regarde que la police du corps ne la voit pas. AUCUNE police nouvelle à installer : DejaVu, Symbola et Noto Sans CJK, les trois familles que le préambule nomme déjà, couvrent tout ; ce qui manquait n'était pas une police mais une CORRESPONDANCE, XeLaTeX ne se rabattant jamais tout seul. PIÈGE DÉCOUVERT EN CHEMIN : clautolisp-doc-preamble.sty existe CINQ fois, une par sous-projet, chaque build chargeant la copie voisine de ses sources, et rien ne les tenait synchronisées — ça m'a mordu pendant le correctif (⚘ ajouté à la copie alfe, le document clautolisp continuait de perdre le caractère, build toujours vert). scripts/check-doc-glyph-coverage.py (make check-doc-glyph-coverage, exécuté par le job documentation AVANT le build) lit les sources et le préambule et demande à fontconfig quelles familles ont réellement chaque point de code, plutôt que de compiler chaque PDF — ce qui serait lent ET ne prouverait rien que sur les polices de la machine qui l'a lancé ; il exige aussi la couverture dans les DEUX fontes (serif et mono) et refuse toute dérive entre les cinq copies. Aujourd'hui : 52 points de code non-ASCII distincts sur 63 sources, tous couverts ou mappés. Le job CI installe désormais les polices (fonts-dejavu, fonts-symbola, fonts-noto-cjk) et fontconfig au lieu d'espérer que l'image les ait — chaque repli du préambule étant gardé par IfFontExistsTF, une police absente dégrade en glyphe perdu dans un PDF qui se construit quand même. NON TRAITÉ et signalé : le [undefined reference] du même avertissement, qui est un défaut distinct (références croisées cassées dans la source org) |

** Interactor / UX
| Issue                               | Status   | Note |
|-------------------------------------+----------+------|
| dribble                             | DONE     | FERMÉ 2026-08-11 sur instruction pjb, après vérification bout-en-bout sur le binaire construit (pas seulement les 14 tests FiveAM) : chemin XDG par défaut au format ISO-8601, --dribble=FILE, append, en-tête ;; H:, (clal-dribble "p") -> chemin ABSOLU, (clal-dribble) -> nil + fermeture, lignes ;; O: / ;; E: / ;; C: conformes à l'Exemple du ticket, --dribble-interactors=t vs =autolisp,dbg (le filtrage nommé discrimine bien : DBG enregistré, NAV non), *clal-dribble-interactors* honoré depuis Lisp, pause sur interactor étranger. La moitié **alfe** n'existe pas (grep dribble dans autolisp-front-end/ = 0 fichier) — rehomée en alfe-dribble.issue plutôt que de garder ce ticket ouvert sur un reliquat de portée différente. issues/closed/ |
| alfe-dribble                        | TODO     | NOUVEAU 2026-08-11 : moitié alfe de dribble. CADRAGE pjb : « dribble sur alfe signifie que la fonction travaille avec bricscad et autocad ; alfe --clautolisp surement l'implemente deja dans clautolisp ». Donc DEUX travaux distincts — backend clautolisp = simple pass-through du flag dans l'argv spawné (BUILD-SUBPROCESS-ARGV le fait déjà pour -Esource, précédent à copier ; attention au --no-init imposé) ; backends bricscad/autocad = tout est à construire côté alfe, ces moteurs n'ont aucun dribble. Les slots CLI existent déjà côté autolisp-cli (« clautolisp today, alfe planned »). Spécificités : sous-répertoire par backend, en-tête à DEUX versions (il faut interroger le backend vivant) |
| dribble-eof-prompt-recorded         | DONE     | NOUVEAU 2026-08-11 : le prompt en attente à EOF est enregistré en `;; O: _$', contredisant la règle « prompts are not recorded » du ticket dribble. Attention : PAS un simple :discard à passer — au EOF rien ne prouve que la partielle est un prompt, et un (princ "sans newline") final est de la vraie sortie. Tradeoff conservateur assumé, WONTFIX défendable. FAIT 2026-08-19 (1.8.53) : CLAUTOLISP.INTERACTOR:*WRITING-PROMPT*, liée par INTERACTOR-LOOP autour du prompt qu'elle imprime et par WITH-DRIBBLE-PROMPT autour des écritures du lecteur REPL ; le tee suit si la ligne ouverte est PUREMENT du prompt (un seul caractère non-prompt l'annule — une ligne en partie réelle est réelle). La variable vit dans le paquet interacteur et non dans le dribble : savoir quels caractères sont un prompt n'est connu que de la boucle, en aval un prompt est indiscernable d'un princ sans retour ligne. CE QUE LE PREMIER ESSAI A RATÉ : placer la décision dans DRIBBLE-STOP NE MARCHE PAS, le repro imprimait toujours la ligne ; en instrumentant, il n'y a PLUS de ligne ouverte à cet instant — elle a déjà été émise par la règle d'ENTRELACEMENT. Décider au stop, c'était décider trop tard. La décision est donc dans %TEE-EMIT-BUFFERED-LINE, seul point de passage de toutes les terminaisons, ce qui est aussi la lecture fidèle de la règle « les prompts ne sont pas enregistrés », où que la ligne finisse. Seul le repro du ticket contre un binaire construit l'a montré. 4 tests (92 vérifications vertes), dont la garde de régression du mauvais placement |
| clal-save-aldo-configuration-undocumented-file | DONE | NOUVEAU 2026-08-14 : (clal-save-aldo-configuration) et `,settings save' écrivent le MÊME aldo.conf mais plus de la même façon depuis 1.8.30 — la version AutoLISP écrit un sexp nu, sans en-tête ni défauts commentés, donc elle dépouille silencieusement la doc d'un fichier que l'utilisateur éditait à la main. Rien ne casse (relecture identique). Le correctif évident (appeler WRITE-CONFIGURATION-FILE) est une VIOLATION DE COUCHE : autolisp-builtins-core ne dépend pas de autolisp-debug-ui, et la dépendance va dans l'autre sens. Écart sémantique en plus : *clal-aldo-configuration* est une valeur AutoLISP arbitraire, pas le store de réglages. Reco = option 2 (hook posé par la couche UI au démarrage, comme *default-on-quit-policy* pour sedit). FAIT 2026-08-19 (1.8.53), option 2 : *ALDO-CONFIGURATION-WRITER* dans autolisp-builtins-core (NIL, ou une fonction d'un flux), consultée par SAVE-ALDO-CONFIGURATION-TO et posée au démarrage par l'outil clautolisp, la couche qui voit les deux côtés — aucune dépendance nouvelle. LE FOSSÉ SÉMANTIQUE que le ticket signalait est fermé et non maquillé : le crochet sérialise (clautolisp.debug.ui:read-config-variable), c'est-à-dire la VARIABLE AUTOLISP lue par le pont déjà existant, et non *aldo-configuration* ; sauver depuis AutoLISP sauve donc toujours ce qu'AutoLISP tient, seule la FORME change. 4 tests : en-tête et défauts commentés présents AVEC les valeurs acceptées (ce qui rend le fichier éditable sans le manuel à côté), un réglage posé écrit en donnée VIVE et non en commentaire de plus, le fichier se relit comme exactement la configuration sauvée (1 entrée, pas 15 — les défauts documentés restent des commentaires), et sans le crochet la forme nue est encore écrite et se relit : le repli est un repli, pas un échec |
| dribble-stop-announcement-interactor | DONE    | stop-announcement in the interactor — pjb l'a marqué « very low priority, I'm not sure it needs to be changed ». REPRODUIT 2026-08-11 exactement comme décrit (les 3 lignes DBG> de l'annonce passent le filtre AUTOLISP-only, l'interaction DBG/NAV qui suit non) : le diagnostic du ticket tient. RÉSOLU 1.9.5 (MR !135) : seam de protocole CALL-WITH-STOP-INTERACTOR — SESSION-STOP enrobe tout le stop (annonce comprise) sous l'activation ALDO du dumb-UI (push sorti de UI-AWAIT-COMMAND), donc le filtre voit ALDO dès la première ligne et l'annonce ne fuit plus. Test de régression DRIBBLE-OMITS-DEBUGGER-STOP-ANNOUNCEMENT (échoue quand on désactive le push). issues/closed/ |
| bang                                | DONE     | NOUVEAU 2026-08-15 (pjb) : caractère d'échappement shell « ! » en tête de ligne, dans TOUS les interactors (lisp, aldo, navi, sedit) — le reste de la ligne part au $SHELL avec stdin/stdout/stderr HÉRITÉS, ce que startapp ne fait pas. Valeur configurable `shell-escape-character'. Quatre détails qui font le travail : le « ! » doit être le PREMIER caractère (aucun espace avant) ; une commande finissant par « & » doit se voir ajouter `echo $!' car bash non interactif n'affiche ni PID ni instruction de contrôle de tâches, et l'ordre des deux sorties n'est pas garanti (le ticket montre les deux) ; des espaces sont permis avant/après la commande, donc le test du « & » doit les tolérer ; et les lignes de continuation par « \ » en fin de ligne doivent être recollées. La colonne « what is executed » est un exemple : la commande réelle dépend de $SHELL. **COLLISION DE DÉFAUTS avec l'autre ticket (pjb, 2026-08-15)** : bang ET cadtui prennent « ! » comme caractère d'échappement PAR DÉFAUT — bang pour le shell, cadtui pour les commandes (TUI-COMMAND-ESCAPE ASCII "!", § Configuration du caractère d'échappement). Sous --host cadtui il faudrait donc taper deux « ! » pour atteindre le shell, et cadtui donne justement au doublement en tête de ligne un AUTRE sens (échapper l'échappement, § principe 4) — les deux règles se disputent la même frappe. Les deux sont configurables, donc c'est réparable ; mais c'est une DÉCISION à prendre (défauts distincts, ou ordre de préséance explicite entre la couche interactor et la couche host), pas quelque chose à laisser au configurateur. À trancher avant d'implémenter l'un ou l'autre. **RÉSOLU 1.8.37.** Implémenté avec le réglage `shell-escape-character' dans lisp.conf, type :character acceptant une chaîne d'un caractère OU un point de code décimal — les deux mêmes écritures que les glyphes :decorations d'aldo.conf, pour garder une seule convention. DEUX HOOKS et pas de code dans l'interactor : ce système est délibérément sans dépendances (« reusable outside the debugger »), or lancer un processus tirerait uiop et lire le réglage tirerait la couche configuration qui est AU-DESSUS ; le tool installe les deux au démarrage, comme *DEFAULT-ON-QUIT-POLICY* pour sedit, et l'un ou l'autre à NIL désactive l'échappement. Le caractère est lu par une FONCTION et non copié au lancement, donc `,set shell-escape-character' prend effet à la ligne suivante. LE DÉTAIL QUI A DICTÉ LA FORME : « no space before the escape character » — or les lecteurs existants passent par %READ-COMMAND-LINE qui ROGNE À GAUCHE, donc tester là aurait accepté « !pwd » précédé d'un espace ; les lecteurs prennent désormais la ligne BRUTE et ne rognent qu'après le test. TROUVÉ EN CHEMIN : la suite INTERACTOR-SUITE ne tournait PAS — absente de l'agrégat clautolisp/tests, framework et suite existaient mais rien ne les invoquait ; câblée, 190 checks dont tous les anciens jamais exécutés en CI. 8 tests neufs, SBCL+CCL exit 0. issues/closed/ **COLLISION RÉSOLUE 2026-08-15 (pjb)** : cadtui prend « = » comme caractère d'échappement des méta-commandes ; « ! » reste au shell (bang) et « , » aux commandes clautolisp. Répartition des débuts de ligne dans la console cadtui : ( = expression Lisp, `,' = commande clautolisp, ! = shell, = = méta-commande cadtui, . - _ ou rien = commande CAD. Écartés en cours de route, et pour de bonnes raisons : « @ » est le préfixe des COORDONNÉES RELATIVES d'AutoCAD/BricsCAD (@10,5) — la saisie la plus fréquente d'une ligne de commande CAD, et la spec montre justement l'utilisateur taper 10,5 en mode Étirer — et « # » celui des coordonnées absolues ; « , » partagé aurait mis DEUX grammaires derrière un préfixe, les méta-commandes étant des appels verbe(arg, nommé: v) avec imbrication, les commandes d'interactor des suites de mots soumises à la règle §0 des initiales. Spec mise à jour (principe 4, grammaire, répertoire, session, checklist) ; le caractère étant aussi le délimiteur verbatim d'org, les références passent en balisage ~…~. |
| navi-double-quit                    | DRAFT    | NOUVEAU (pjb), INDEXÉ 2026-08-15 — il manquait au triage, comme navi-sedit-eval-command : tous deux déposés hors de la boucle de resync. `q' dans NAVI devrait aussi quitter ALDO ; aujourd'hui il faut un `q' par interactor empilé (la trace du ticket montre NAV> q puis DBG> q pour revenir à _$). Le ticket se dit lui-même DRAFT : les étapes de reproduction exactes et la SÉMANTIQUE voulue restent à trancher (un seul q sort de tout, ou deux commandes distinctes) — c'est une décision, pas un bug à corriger tel quel. Touche la même zone que cadtui, qui empile un interactor de plus : à trancher AVANT, sinon la règle sera fixée par accident. **Bug distinct visible dans la trace, à ne pas perdre** : le fichier ouvert par CLAL-SEDIT s'appelle littéralement `"~/src/public/autolisp/fact.lsp"' — guillemets CONSERVÉS dans le nom et `~' NON développé, le tout recollé au cwd. À vérifier séparément |
| navi-sedit-eval-command             | DRAFT    | NOUVEAU (pjb), INDEXÉ 2026-08-15 — commande « évaluer la sélection » dans navi et sedit. DRAFT assumé : à spécifier (la touche, ce que « sélection » veut dire dans navi vs dans sedit, où le résultat s'affiche, le traitement des erreurs). Dépend de la même question que ci-dessus si l'évaluation peut ouvrir un débogueur imbriqué |
| interactor-state-diagram-revision   | DEFERRED | pjb-decision |

** Big features / environment
| Issue                    | Status   | Note |
|--------------------------+----------+------|
| cador-multidocument-host | TODO     | multi-document host (cador) — DECOMPOSED 2026-08-06 into cador-1..4 (see §0 of the issue). cador-1 (P1) + cador-2 (P1) + cador-3 (P2) + cador-4 (P3) below |
| cador-4-epic-remainder   | DEFERRED | DB backend / command layer / §7.4 adapt / locking / conformance E1–E10 / wire host — gated on cador-1..3 + CAD access |
| salve-environment        | DEFERRED | SPEC draft; pjb-decision (SLIME-like Emacs env; aldb ⊂ salve) |
| alfe-plugin-model        | DEFERRED | V2 plugin contract + EPURE (3 open design decisions) |
| cadtui                   | TODO     | NOUVEAU 2026-08-15 (pjb) — GROS MORCEAU. Implémenter le host `cadtui' : une interface UTILISATEUR TEXTUELLE pour les objets CAD et pour l'application CAD (clautolisp). Spécification de 882 lignes dans clautolisp/cadtui/documentation/cadtui-specifications.org, plus prompt-claude-code.md et CLAUDE.md dans le même répertoire. Couvre : étude des interfaces AutoCAD/BricsCAD retenues, arborescence des objets d'interface, communication inter-dessins (tableau noir), modèle de données CLOS, langage d'ordres d'interaction (adressage des cibles, méta-commandes, touches, saisie implicite de commandes et d'expressions Lisp, classification et tamponnage des lignes, un seul thread actif à la fois), et toute une partie LOCALISATION avec trois dictionnaires distincts pour les commandes CAD plus un pour les méta-commandes. POINT D'ARCHITECTURE signalé par pjb : ces spécifications GÉNÉRALISENT celles du host cador, donc prévoir du refactoring de cador et des sous-modules communs — à décider avant de coder, pas après. Le ticket demande explicitement une PLANIFICATION d'abord, incluant des tâches de recherche pouvant utiliser le CI et les runners macos/windows pour relever les noms LOCALISÉS des commandes (cf. documentation/ci-job-durations.md : un balayage CAD complet ≈ 30 min de runner exclusif, et la boîte Windows est un portable parfois sur batterie — à budgéter dans le plan). **COLLISION DE DÉFAUTS avec l'autre ticket (pjb, 2026-08-15)** : bang ET cadtui prennent « ! » comme caractère d'échappement PAR DÉFAUT — bang pour le shell, cadtui pour les commandes (TUI-COMMAND-ESCAPE ASCII "!", § Configuration du caractère d'échappement). Sous --host cadtui il faudrait donc taper deux « ! » pour atteindre le shell, et cadtui donne justement au doublement en tête de ligne un AUTRE sens (échapper l'échappement, § principe 4) — les deux règles se disputent la même frappe. Les deux sont configurables, donc c'est réparable ; mais c'est une DÉCISION à prendre (défauts distincts, ou ordre de préséance explicite entre la couche interactor et la couche host), pas quelque chose à laisser au configurateur. À trancher avant d'implémenter l'un ou l'autre. **COLLISION RÉSOLUE 2026-08-15 (pjb)** : cadtui prend « = » comme caractère d'échappement des méta-commandes ; « ! » reste au shell (bang) et « , » aux commandes clautolisp. Répartition des débuts de ligne dans la console cadtui : ( = expression Lisp, `,' = commande clautolisp, ! = shell, = = méta-commande cadtui, . - _ ou rien = commande CAD. Écartés en cours de route, et pour de bonnes raisons : « @ » est le préfixe des COORDONNÉES RELATIVES d'AutoCAD/BricsCAD (@10,5) — la saisie la plus fréquente d'une ligne de commande CAD, et la spec montre justement l'utilisateur taper 10,5 en mode Étirer — et « # » celui des coordonnées absolues ; « , » partagé aurait mis DEUX grammaires derrière un préfixe, les méta-commandes étant des appels verbe(arg, nommé: v) avec imbrication, les commandes d'interactor des suites de mots soumises à la règle §0 des initiales. Spec mise à jour (principe 4, grammaire, répertoire, session, checklist) ; le caractère étant aussi le délimiteur verbatim d'org, les références passent en balisage ~…~. **NOTE D'IMPLÉMENTATION (pjb, 2026-08-15)** : la console cadtui est un INTERACTOR au sens du cadre existant (INTERACTOR-LOOP, celui du REPL lisp, d'aldo, du navigateur, de l'inspecteur et de sedit), pas un lecteur de lignes ad hoc. Conséquence directe et voulue : on peut PASSER D'UN INTERACTOR À L'AUTRE — la console s'empile et se dépile comme les autres, et depuis elle on descend dans aldo ou sedit et on revient, sans mécanisme nouveau. Impose que la classification des lignes soit le READER de cet interactor (à la place de COMMA-COMMAND-READ pour le REPL ou COMMAND-READ pour aldo/navi/sedit — trois variantes d'un même point d'extension, pas trois boucles), et que le caractère d'échappement, la locale et la pagination soient des réglages d'interactor empilés comme lisp.conf sous aldo.conf, pas des variables globales. |

* Companion non-issue files (keep, do not process as issues)
=alfe.plan=, =opendcl.plan=, =missing-functions-plan.md=,
=note-secureload.txt=, =schms-call-inventory.md=,
=test-1252.lsp=, =compiler-transpile-probe.lisp=.
